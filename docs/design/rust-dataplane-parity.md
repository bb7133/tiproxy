# Rust dataplane parity manifest

## Purpose and baseline

This manifest is the auditable contract for replacing TiProxy's Go SQL
dataplane with Rust. It describes externally observable behavior and lifecycle
invariants, not a proposed implementation. A Rust implementation is not at
parity merely because common queries work: every applicable item below must
have a passing target test and must satisfy its release gate.

The source inventory was taken from commit
`e3371098480c4d644b070544f9c1aef63fcee9b1` on `main`. When Go behavior changes,
the author of that change must update this file and the referenced parity test
in the same pull request.

The architectural boundary is fixed:

- Rust owns SQL listeners, frontend and backend sockets, TLS, compression,
  PROXY protocol, MySQL packets, command response state, and session migration.
- Go owns discovery, routing, balancing, configuration, certificates, API, and
  VIP management through a bounded, versioned control protocol.
- MySQL packet payloads must never cross IPC, cgo, or FFI.
- Traffic capture and replay is the only functional exclusion. Rust dataplane
  mode must fail fast when it is enabled; it must not silently omit capture.

## How to read the manifest

IDs are permanent. Removed behavior keeps its row and becomes `RETIRED` with a
link to the decision; IDs are never reused. The target test ID is derived from
the item ID as `PARITY-<item-id>` and is shared by the protocol corpus,
integration tests, and canary reports.

Status values:

| Status | Meaning |
| --- | --- |
| `GO-VERIFIED / RUST-TODO` | Go source and a focused Go test exist; Rust parity is not yet demonstrated. |
| `GO-IMPLEMENTED / RUST-TODO` | Go source exists but focused Go coverage is missing or indirect. The parity test must first freeze the observed behavior. |
| `DECISION-REQUIRED` | Go behavior is ambiguous, accidental, or unsafe to copy. Resolve before implementing Rust. |
| `PARITY-VERIFIED` | The referenced differential/integration test passes for Go and Rust at the recorded revisions. |
| `RUST-PARTIAL` | Rust implements and verifies a bounded subset of the Go behavior; the row states the remaining gap. |
| `EXCLUDED` | Deliberately outside the Rust dataplane contract. |
| `RETIRED` | No longer required; the row must link to the approved removal. |

Severity is evaluated separately for canary and cutover:

| Severity | Canary gate | Cutover gate |
| --- | --- | --- |
| `P0` | Blocks any production canary. | Blocks cutover and requires rollback if detected. |
| `P1` | Allows only an explicitly scoped diagnostic canary; blocks expansion. | Blocks cutover. |
| `P2` | May canary with an owner and alert. | Requires an accepted exception or completion before general availability. |

`Rust owner` names the crate or boundary responsible for the behavior. It is
not a person assignment.

`tests/dataplane/differential` enforces that every permanent ID is either
referenced by the immutable Go v1 corpus or has one concrete, reviewable entry
in `parity-exclusions.json`. Corpus references to unknown IDs, stale
exclusions, and newly added rows with neither coverage nor an exclusion all
fail CI. The sharded Rust consumer replays covered traces through merged
parser/FSM code and reports the first case, packet index, state/effect, and
expected/observed semantic field without emitting packet payloads.

## Audited Go source inventory

The manifest audit covers all production files under these paths. Test and
configuration references in individual rows provide the executable evidence.

| Area | Audited source |
| --- | --- |
| Listener and connection lifecycle | `pkg/proxy/proxy.go`, `pkg/proxy/client/client_conn.go`, `pkg/proxy/errno.go` |
| Handshake and sessions | `pkg/proxy/backend/authenticator.go`, `backend_conn_mgr.go`, `cmd_processor*.go`, `handshake_handler.go`, `static_handshake_handler.go`, `error.go`, `metrics.go` |
| MySQL wire | all production files in `pkg/proxy/net/` |
| Keepalive | all production files in `pkg/proxy/keepalive/` |
| PROXY protocol v2 | all production files in `pkg/proxy/proxyprotocol/` |
| Dataplane metrics | `pkg/metrics/backend.go`, `balance.go`, `server.go`, `session.go`, `traffic.go`, `metrics.go` |
| Dataplane configuration | `lib/config/proxy.go`, `security.go`, `namespace.go`, `balance.go` |
| Routing integration | `pkg/balance/router/`, `pkg/manager/namespace/`, `pkg/manager/backendcluster/` |
| Explicit exclusion | `pkg/sqlreplay/`, `pkg/metrics/replay.go`, `docs/design/2024-08-27-traffic-replay.md` |

## Packet and transport parity

| ID | Required behavior and invariant | Go source | Go test evidence | Rust owner | Target test ID | Status | Canary | Cutover |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| PKT-001 | Read and write the 4-byte MySQL physical-packet header, enforce 24-bit payload length, and advance the 8-bit sequence exactly once per physical packet. | `pkg/proxy/net/packetio.go` (`ReadPacket`, `WritePacket`) | `pkg/proxy/net/packetio_test.go:TestPacketIO`, `TestPredefinedPacket`, `TestPacketSequence`; Rust `rust/crates/mysql-wire/tests/corpus.rs` (Go-corpus header/sequence/split/empty vectors) + `rust/crates/proxy-io/tests/corpus_streaming.rs` (loads the Go traces through the streaming forwarder); `rust/crates/dataplane/tests/session_engine.rs:packet_framing_splits_and_realigns_through_production_engine` (physical-packet count + `sequence_mismatches()==0` from the reader's `ForwardProgress` + per-command sequence realignment through the live engine) | `mysql-wire` | `PARITY-PKT-001` | GO-VERIFIED / RUST-VERIFIED (the production forward path emits exact 4-byte header / 24-bit length / once-per-physical-packet 8-bit sequence: a MAX-1 response is one physical packet and MAX+33 is two, each realigning the next command at a clean sequence boundary, verified through the live session engine over real sockets with proxy-io codec byte-equivalence against the Go traces) | P0 | P0 |
| PKT-002 | Assemble and emit logical packets split at `MaxPayloadLen`, including the zero-length terminator for exact multiples. | `pkg/proxy/net/packetio.go` | `pkg/proxy/net/packetio_test.go:TestPacketIO`, `TestForwardUntilLongData`, `TestForwardPacketToLongData`; Rust `rust/crates/proxy-io/tests/corpus_streaming.rs` (PARITY-PKT-002 split/exact-multiple-terminator codec vectors); `rust/crates/dataplane/tests/session_engine.rs:packet_framing_splits_and_realigns_through_production_engine` (MAX+33 to two physical packets, exact MaxPayloadLen adds the zero-length terminator, through the live engine) | `mysql-wire` | `PARITY-PKT-002` | GO-VERIFIED / RUST-VERIFIED (a MAX+33 logical response splits into exactly two physical packets and an exact-MaxPayloadLen response emits the required zero-length terminator when forwarded through the production engine; mutation-checked that shrinking the exact-multiple response below the boundary drops the terminator packet, and Go golden split/terminator vectors match at the codec layer) | P0 | P0 |
| PKT-003 | Stream a command whose first physical packet is at least `MaxPayloadLen` without materializing the logical payload; retain at most the first 1024 bytes needed for command state. | `pkg/proxy/backend/backend_conn_mgr.go:ExecuteCmd`; `pkg/proxy/net/packetio.go:ForwardPacketTo` | `pkg/proxy/backend/backend_conn_mgr_test.go:TestExecuteCmdStreamingForwardLargeQuery`; `pkg/proxy/net/packetio_test.go:TestForwardPacketToLongData`; `rust/crates/dataplane/tests/session_engine.rs:oversized_command_streams_to_the_backend_without_being_materialized`; `rust/crates/dataplane/src/session_engine.rs:oversized_headers_stream_unless_the_command_must_be_rewritten` | `dataplane`, `mysql-wire` | `PARITY-PKT-003` | GO-VERIFIED / RUST-VERIFIED (**the follow-up this row demanded has landed; it was not liftable by the earlier evidence and was not lifted by it.** Production command intake no longer reads the request first: `session_engine.rs:classify_intake` decides from the peeked header alone, and a command whose first physical packet already reaches `MAX_PHYSICAL_PAYLOAD_LEN` is relayed through `PacketIo::forward_packet_to` with a `STREAMED_COMMAND_CAPTURE` = 1024 bound -- the same threshold and the same 1024 Go uses in `forwardCommand`. `COMMAND_PAYLOAD_LIMIT` is still the ceiling for a materialized command and is now simply never reached by a streamed one. The end-to-end test sends a request **larger than `COMMAND_PAYLOAD_LIMIT` itself**, which is what makes the no-materialize property observable rather than asserted: under the materializing path `read_logical` refuses it and no OK returns, and that is exactly how the negative control fails. Both ends of the test stream too -- the client writes from a generator, the backend drains into a sink -- so the proxy is the only participant that could have held 64 MiB. Every byte of the logical request is accounted for at the backend and the session stays in wire sequence for the next command. The two facts an end-to-end test cannot reach cheaply are driven directly against `classify_intake`, each with its own negative control: dropping the `COM_CHANGE_USER` exemption makes it stream (it must not -- that packet is rewritten before forwarding, so it has to be read in full at any size), and neutralizing the threshold makes a below-threshold command stream. **Approved safety divergence from Go, agreed with @CodexM5**: Go's `ExecuteCmd` reads `binary.LittleEndian.Uint16(request[1:])` for `COM_SET_OPTION` from the outer `request`, which is `nil` in streaming mode, so a `≥MaxPayloadLen` `COM_SET_OPTION` panics Go. Rust does not reproduce that: the state effects are re-derived from the captured prefix after the relay, and a prefix that does not decode fails the session closed. A second, smaller divergence in the same direction: an oversized packet whose first byte is not a command is refused from the header, where Go streams it to the backend -- reading it only to reject it would reintroduce the allocation this row exists to remove. Go's `!streamingForward` gate on the redirect `BEGIN` hold is mirrored exactly; a maximal first packet cannot be the `BEGIN` that path exists to hold) | P0 | P0 |
| PKT-004 | On an unexpected physical-packet sequence, log the mismatch and resynchronize to the received sequence plus one, matching Go's tolerant behavior. Reset both streams before each command and preserve client-versus-backend error attribution for actual I/O failures. | `pkg/proxy/net/packetio.go:consumeIncomingPhyHeader`; `pkg/proxy/client/client_conn.go:processMsg` | `pkg/proxy/net/packetio_test.go:TestPacketSequence`; `pkg/proxy/backend/cmd_processor_test.go:TestNetworkError` | `mysql-wire`, `session-core` | `PARITY-PKT-004` | GO-IMPLEMENTED / RUST-PARTIAL (DPL-04: the production engine resets all four per-channel sequences at every command boundary, relays the connection phase in lockstep, and continues LOCAL INFILE lineages; verified against strict fake-peer header-sequence assertions in `rust/crates/dataplane/tests/session_engine.rs` — reader-side tolerant resync remains observe-only) | P0 | P0 |
| PKT-005 | Treat empty logical payloads as valid packets and reject a command packet shorter than one byte as malformed. | `pkg/proxy/net/packetio.go`; `pkg/proxy/backend/backend_conn_mgr.go:ExecuteCmd` | `pkg/proxy/net/packetio_test.go:TestReadPacketEmptyPayload`; `pkg/proxy/backend/cmd_processor_test.go:TestNetworkError`; Rust `rust/crates/proxy-io/tests/corpus_streaming.rs` (empty logical payload valid); `rust/crates/dataplane/tests/session_engine.rs:empty_command_payload_fails_closed_before_backend_write` (a zero-length command fails the session closed before any backend write) | `mysql-wire`, `session-core` | `PARITY-PKT-005` | GO-VERIFIED / RUST-VERIFIED (a zero-length command carries no command byte and the production engine fails the session closed before any backend write, matching Go's rejection of a sub-one-byte command packet; empty logical payloads remain valid packets in stream contexts per the proxy-io corpus) | P1 | P0 |
| PKT-006 | Preserve configurable connection-buffer sizing, pooling, direct/streaming writes, byte counters, flush behavior, and bounded post-TLS buffers. | `pkg/proxy/net/packetio.go`, `tls.go`; `lib/config/proxy.go:ProxyServerOnline.ConnBufferSize` | `pkg/proxy/net/packetio_test.go:TestPacketIOPooling`, `TestPoolSizeMismatch`; `pkg/proxy/net/tls_test.go:TestTLSBufferSizes`, `TestPacketIOTLSBufferSizes`; `lib/config/proxy_test.go:TestProxyCheck`; `rust/crates/proxy-io/tests/pump.rs:parity_pkt_006_pool_reuses_fixed_buffers_with_bounded_idle_memory`, `rejects_unbounded_or_impossible_policies_before_allocation`, `small_reads_batch_and_flush_before_the_latency_bound`, `slow_destination_backpressures_at_the_high_water_mark_and_cancel_joins`; `rust/crates/proxy-io/tests/counted.rs:counts_match_bytes_transferred_and_are_monotonic`, `a_pending_read_counts_nothing_until_it_completes`, `vectored_and_zero_length_writes_count_exactly_what_transferred`, `probe_try_read_counts_only_consumed_bytes`; `rust/crates/proxy-io/src/tls.rs:buffer_sizes_match_go_clamp_semantics`; `rust/crates/proxy-io/tests/tls_matrix.rs:raw_counter_below_tls_counts_record_bytes_not_plaintext`, `raw_counter_is_not_double_counted_by_prefix_replay` | `proxy-io` | `PARITY-PKT-006` | GO-VERIFIED / RUST-VERIFIED (pooling returns fixed-size buffers with bounded idle memory, and an unbounded or impossible policy is refused *before* allocation rather than after. Flush behavior is the batching one Go has: small reads coalesce and still flush before the latency bound, and a slow destination backpressures at the high-water mark instead of growing without limit. Byte counters are exact at the awkward edges -- a read that is still pending counts nothing, vectored and zero-length writes count exactly what transferred, and a `try_read` probe counts only what it consumed. Post-TLS buffers reproduce Go's clamp arithmetic including both ends: zero normalizes to the 32-KiB default giving (4 KiB, 16 KiB), small sizes clamp up to the 1-KiB minimum, and huge sizes stay capped. The counter below TLS counts record bytes rather than plaintext, and a replayed buffered prefix is not double-counted) | P1 | P1 |
| PKT-007 | Preserve disconnect classification for EOF, reset, closed-connection, timeout, and wrapped frontend/backend I/O failures. | `pkg/proxy/net/error.go`; `pkg/proxy/backend/error.go` | `pkg/proxy/net/error_test.go:TestIsDisconnectErr`; `pkg/proxy/backend/cmd_processor_test.go:TestNetworkError`; `rust/crates/session-core/src/error_source.rs:is_disconnect_io_matches_go_disconnect_set`, `is_disconnect_io_unwraps_custom_non_io_wrappers`; `rust/crates/dataplane/tests/session_engine.rs:backend_greeting_transport_break_is_backend_network_on_both_observables`, `backend_to_client_destination_write_break_is_client_network_on_both_observables` | `proxy-io`, `session-core` | `PARITY-PKT-007` | GO-VERIFIED / RUST-VERIFIED (the disconnect set is Go's exactly -- EOF, EPIPE, ECONNRESET, ECONNABORTED, ETIMEDOUT -- and the test also pins the **complement**: `NotFound`, `InvalidData`, `PermissionDenied`, `WriteZero` and `Other` must NOT classify as disconnects, because they fall through to the wrapped proxy/malformed classes just as they do in Go. Classification is on `ErrorKind` rather than raw errnos, which is what makes it portable. The wrapping clause is the subtle one and it is held twice: Go's `errors.Is` unwraps through any `Unwrap()`-able wrapper, so a disconnect nested inside an `io::Error(Other)` classifies, and so does one under a custom non-io wrapper that exposes it only through `source()` -- a `get_ref()`-only walk would miss the second. Frontend versus backend attribution is proven end-to-end: a break at the backend greeting reports backend-network and a break writing to the client reports client-network, on both observables) | P1 | P1 |
| PP2-001 | In inbound fallback mode, recognize the v2 signature, parse LOCAL/PROXY command, IPv4/IPv6/Unix addresses, stream/datagram family, and TLVs; otherwise leave MySQL bytes untouched. | `pkg/proxy/net/proxy.go`; `pkg/proxy/proxyprotocol/listener.go`, `proxy.go` | `pkg/proxy/net/proxy_test.go:TestProxyParse`, `TestProxyReadWrite`; `pkg/proxy/proxyprotocol/listener_test.go:TestProxyListener`; `pkg/proxy/proxyprotocol/proxy_test.go:TestProxyParse` | `proxy-io` | `PARITY-PP2-001` | PARITY-VERIFIED | P0 | P0 |
| PP2-002 | Emit v2 PROXY command to TiDB, preserving an inbound header and TLVs when present or synthesizing source/destination addresses when absent. | `pkg/proxy/backend/authenticator.go:writeProxyProtocol`; `pkg/proxy/net/proxy.go` | `pkg/proxy/backend/authenticator_test.go:TestProxyProtocol`; `pkg/proxy/net/proxy_test.go:TestProxyReadWrite` | `proxy-io`, `dataplane` | `PARITY-PP2-002` | PARITY-VERIFIED (WIRE-B + WIRE-05 closure: `PacketIo` consumes a complete inbound header exactly once without advancing MySQL sequence state, retains its bounded on-wire image with redacted `Debug`, and every initial/redirect backend dial changes only the command nibble to `PROXY`; family, transport, address body, Unix/UNSPEC quirks, TLV bytes, and TLV order stay verbatim. Headerless sessions still synthesize the real peer→dial destination. Locked by `inbound_proxy_v2_probe_matrix`, `inbound_proxy_v2_forwarding_preserves_wire_except_command`, `inbound_proxy_v2_header_and_tlvs_are_forwarded_verbatim`, and `outbound_proxy_v2_header_counts_once_in_raw_backend_out`; real TiDB `proxy` and `tls-proxy-zstd` variants prove PROXY-before-greeting/TLS and successful post-auth traffic.) | P0 | P0 |
| PP2-003 | Surface proxy/TiDB configuration mismatches, including TLS failures caused by an unexpected v2 header, as backend handshake failures. Review note F2: Go partly classifies the PPV2 mismatch by substring-matching "PROXY Protocol" in TiDB's generic 1105 message; Rust must replicate this classification input until a structured error replaces it. | `pkg/proxy/backend/authenticator.go:handleHandshakeError`, `writeAuthHandshake` | `pkg/proxy/backend/authenticator_test.go:TestProxyProtocol` | `session-core` | `PARITY-PP2-003` | PARITY-VERIFIED (`classify_backend_auth_packet` matches Go's 1156/8052 codes plus the code-independent `PROXY Protocol` message marker, and `AuthRelay` maps only a suspect first backend packet to `BackendProxyProtocol` while forwarding the original ERR; later/non-suspect ERR packets remain authentication failures. Exact evidence: `classifier_matches_go_rules`, `error_routes_match_handle_handshake_error`; marker-removal mutation is killed.) | P1 | P1 |
| TLS-001 | Advertise frontend SSL only when a frontend TLS config exists; accept a 32-byte SSLRequest, complete server TLS using already-buffered bytes, and preserve the TLS state for routing hooks. Review note F1: Go strips the SSL bit with XOR (`^=`), which would accidentally set it for a custom capability mask lacking SSL; Rust must use and-not semantics (approved divergence, never advertise SSL without a TLS config). | `pkg/proxy/backend/authenticator.go:handshakeFirstTime`; `pkg/proxy/net/tls.go:ServerTLSHandshake` | `pkg/proxy/backend/authenticator_test.go:TestEnableTLS`; `pkg/proxy/net/packetio_test.go:TestTLS`; `pkg/proxy/net/tls_test.go:TestTLSReadWrite`; Rust `frontend_tls_capability_and_ssl_request_shape_fail_closed`, `frontend_tls_session_totals_come_from_the_raw_seam_not_framing`, `frontend_accept_replays_buffered_prefix`; real TiUP `tls` and `tls-proxy-zstd` variants | `proxy-io`, `session-core` | `PARITY-TLS-001` | PARITY-VERIFIED (WIRE activation + WIRE-04 closure: the production greeting follows the compiled frontend policy, unsupported or non-exact SSLRequest shapes fail closed before backend contact, exact 32-byte upgrades run real rustls without resetting sequence/raw counters, and already-buffered TLS bytes are replayed once.) | P0 | P0 |
| TLS-002 | Reconcile capabilities from SSLRequest and HandshakeResponse, retaining the SSLRequest mask when drivers omit `CLIENT_SSL` in the second packet. | `pkg/proxy/backend/authenticator.go:handshakeFirstTime` | `pkg/proxy/backend/authenticator_test.go:TestEnableTLS`, `TestCapability`; Rust `tls_reconciliation_trusts_first_packet`, `frontend_tls_capability_and_ssl_request_shape_fail_closed`; corpus `handshake-ssl-request`; real TiUP `tls` authentication matrix | `session-core` | `PARITY-TLS-002` | PARITY-VERIFIED (the strict SSLRequest classifier retains first-packet capabilities across the encrypted response, rejects malformed upgrade shapes, and the live TLS auth matrix completes real native/caching-SHA2 exchanges.) | P0 | P0 |
| TLS-003 | Negotiate backend TLS when both sides permit it; enforce `require-backend-tls`; clone certificates and set SNI from the selected backend hostname. | `pkg/proxy/backend/authenticator.go:verifyBackendCaps`, `writeAuthHandshake`; `lib/config/security.go` | `pkg/proxy/backend/authenticator_test.go:TestEnableTLS`, `TestRequireBackendTLS`; `pkg/proxy/net/packetio_test.go:TestTLS`; Rust `backend_tls_planning_matches_required_and_opportunistic_matrix`, `backend_verification_matches_go`, `wrong_hostname_is_rejected`, `untrusted_ca_is_rejected_on_both_sides`, `redirect_restores_candidate_over_backend_tls`; real TiUP Rust/Go `tls` variants | `proxy-io`, `session-core` | `PARITY-TLS-003` | PARITY-VERIFIED (required mode fails typed without a usable policy and rejects a backend lacking SSL; opportunistic mode enables only for client SSL ∩ backend SSL ∩ compiled policy and otherwise clears SSL. Backend TLS uses the routed host for SNI/verification, is renegotiated on redirect, and is proven against real TiDB in both modes.) | P0 | P0 |
| TLS-004 | Apply configured minimum TLS version, CA/certificate identity, and live certificate rotation consistently to new frontend and backend sessions. | `pkg/manager/cert/`; `lib/config/security.go`; `pkg/proxy/proxy.go:onConn`; `pkg/controlbridge/snapshot.go`; Rust candidate validation in `rust/crates/control-proto/src/snapshot.rs` | `pkg/manager/cert/*_test.go`; `pkg/controlbridge/snapshot_test.go`; Rust `snapshot_reload_preserves_last_good_and_applies_new`, `expired_certificate_is_rejected`, `handshake_timeout_is_typed`, `invalid_server_name_is_typed`, `buffer_sizes_match_go_clamp_semantics`, plus atomic rotate/rename/key-mismatch/CA coverage in `rust/crates/control-proto/tests/snapshot_store.rs` | `dataplane`, `control-proto` | `PARITY-TLS-004` | PARITY-VERIFIED (validated immutable snapshots compile CA/cert/key and TLS 1.2/1.3 floors atomically; invalid reloads preserve last-good, valid rotations affect only new sessions, handshakes are deadline-bounded, and a 32KiB connection buffer clamps TLS read/write allocations to 4KiB/16KiB.) | P1 | P0 |
| CMP-001 | Negotiate classic zlib compression only when supported by client, proxy, and backend; honor MySQL compressed framing, threshold, split, and sequence rules. | `pkg/proxy/backend/authenticator.go:setCompress`; `pkg/proxy/net/compress.go` | `pkg/proxy/backend/authenticator_test.go:TestCompressProtocol`; `pkg/proxy/net/compress_test.go:TestCompressZlib`, `TestCompressMergePkt`, `TestCompressHeader`; `rust/crates/proxy-io/src/compression.rs` unit tests + `rust/crates/proxy-io/tests/compression_go_vectors.rs`; `rust/crates/dataplane/tests/session_engine.rs:compressed_client_zlib_roundtrips_end_to_end`, `compressed_mixed_zlib_client_zstd_backend_roundtrips`, `compressed_large_logical_response_splits_and_realigns` | `mysql-wire`, `session-core` | `PARITY-CMP-001` | PARITY-VERIFIED (Go golden framing/threshold/split vectors and the production `CompressedIo` + `PacketIo` stack cover negotiation, auth-OK activation, per-command reset, and malformed bounds. A client advertising both flags selects zlib by Go precedence while a zstd-only backend independently selects zstd. Under compression, `MAX_PAYLOAD_LEN + 257` consumes exactly two MySQL physical packets, exact `MAX_PAYLOAD_LEN` emits the required zero-length terminator, and the next command remains aligned. Residual `WIRE-03-D3` remains explicit: `zstd-pure-rs` encoder internals contain pinned-version `unsafe`, while peer-controlled decoding stays on the `zstd-zero` path that forbids `unsafe`.) | P0 | P0 |
| CMP-002 | Negotiate zstd, forward the client compression level, and preserve zlib precedence when both capability flags are present. | `pkg/proxy/backend/authenticator.go:setCompress`; `pkg/proxy/net/compress.go` | `pkg/proxy/backend/authenticator_test.go:TestCompressProtocol`; `pkg/proxy/net/compress_test.go:TestCompressZstd`, `TestCompressHeader`; `rust/crates/proxy-io/tests/compression_go_vectors.rs`; `rust/crates/session-core/src/auth.rs:compression_selection_matches_go`; `rust/crates/dataplane/tests/session_engine.rs:compressed_client_zstd_roundtrips_end_to_end`, `compressed_mixed_zlib_client_zstd_backend_roundtrips` | `mysql-wire`, `session-core` | `PARITY-CMP-002` | PARITY-VERIFIED (zstd levels 1–22, zlib precedence, and per-leg selection are locked by Go golden and pure tests plus a runnable mixed session: the frontend uses zlib while the zstd-only backend receives the forwarded level and uses zstd for two commands across a reset.) | P0 | P0 |
| CMP-003 | Maintain compressed and uncompressed sequences across read/write turns, reset before each command, support peek/discard/flush, and classify corrupt frames. | `pkg/proxy/net/compress.go` | `pkg/proxy/net/compress_test.go:TestCompressSequence`, `TestCompressPeekDiscard`, `TestReadWriteError`; `rust/crates/proxy-io/src/compression.rs` unit tests; `rust/crates/dataplane/tests/session_engine.rs:compressed_client_{zlib,zstd}_roundtrips_end_to_end`, `compressed_large_logical_response_splits_and_realigns`, `compressed_local_infile_multi_turn_realigns` | `mysql-wire` | `PARITY-CMP-003` | PARITY-VERIFIED (the shared compressed sequence is reset once per command and slaves the uncompressed sequence at centralized read/write/forward direction changes, with fail-closed buffered-data guards. Under compression, runnable mixed-codec rows cover a >16-MiB/two-physical-packet response, an exact-multiple zero terminator, and LOCAL INFILE's backend request → two client chunks → empty terminator → final OK turns; each immediately serves a next command at a clean boundary.) | P0 | P0 |

## Handshake, authentication, and capability parity

| ID | Required behavior and invariant | Go source | Go test evidence | Rust owner | Target test ID | Status | Canary | Cutover |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| HS-001 | Generate the proxy greeting with connection ID, selected TiDB server version, 20-byte salt, `mysql_native_password`, and the handler's supported capability mask. | `pkg/proxy/backend/authenticator.go:handshakeFirstTime`; `pkg/proxy/net/mysql.go:MakeInitialHandshake` | `pkg/proxy/net/mysql_test.go:TestHandshakeResp`; `pkg/proxy/backend/authenticator_test.go:TestCustomAuth` | `mysql-wire`, `session-core` | `PARITY-HS-001` | GO-VERIFIED / RUST-TODO | P0 | P0 |
| HS-002 | Require `CLIENT_PROTOCOL_41`, negotiate the intersection of client/proxy capabilities, tolerate omitted `CLIENT_PLUGIN_AUTH` for known legacy drivers, and validate backend-required capabilities. | `pkg/proxy/backend/authenticator.go` | `pkg/proxy/backend/authenticator_test.go:TestUnsupportedCapability`, `TestCapability`, `TestUpgradeBackendCap` | `session-core` | `PARITY-HS-002` | GO-VERIFIED / RUST-TODO | P0 | P0 |
| HS-003 | Parse HandshakeResponse auth length variants, optional database, plugin, attributes, and zstd level; reject malformed or over-1-MiB handshake packets without allocation blow-up. | `pkg/proxy/net/mysql.go:ParseHandshakeResponse`; `pkg/proxy/backend/authenticator.go:readClientHandshakePacket` | `pkg/proxy/net/mysql_test.go:TestHandshakeResp`; `pkg/proxy/backend/authenticator_test.go:TestMalformedHandshakePacket`, `TestHandshakePacketSizeLimit` | `mysql-wire`, `session-core` | `PARITY-HS-003` | GO-VERIFIED / RUST-TODO | P0 | P0 |
| HS-004 | Authenticate by sending an unknown backend plugin, forwarding auth-switch challenge/response packets, and supporting `mysql_native_password` and `caching_sha2_password`. Preserve auth bytes so TiDB's `using password` error remains accurate. | `pkg/proxy/backend/authenticator.go:handshakeFirstTime`, `handleHandshakeError` | `pkg/proxy/backend/authenticator_test.go:TestAuthPlugin`, `TestAuthFail` | `session-core`, `mysql-wire` | `PARITY-HS-004` | GO-VERIFIED / RUST-PARTIAL (DPL-04: the engine rewrites the forwarded handshake response to `auth_unknown_plugin` with the original auth data preserved so the backend re-challenges with its own salt, and relays the auth-switch loop; `mysql_native_password` end to end with a non-empty password and `using password` failure semantics in `rust/crates/dataplane/tests/session_engine.rs:non_empty_password_reauths_against_backend_salt`, `wrong_password_reports_using_password`; `caching_sha2_password` remains TODO; the real-TiDB integration topology authenticates through this path end to end: `tests/dataplane/integration/run.sh --mode rust --variant plain`) | P0 | P0 |
| HS-005 | Handle the `caching_sha2_password` fast-auth success marker without sending an extra client packet; forward other SHA commands as the Go state machine does. | `pkg/proxy/backend/authenticator.go:handshakeFirstTime` | `pkg/proxy/backend/authenticator_test.go:TestAuthPlugin` | `session-core` | `PARITY-HS-005` | GO-VERIFIED / RUST-TODO | P0 | P0 |
| HS-006 | Preserve user, database, collation, zstd level, and connection attributes across backend authentication; allow the handshake hook to rewrite user/attributes and select a router. | `pkg/proxy/backend/authenticator.go`; `pkg/proxy/backend/handshake_handler.go` | `pkg/proxy/backend/authenticator_test.go:TestCapability`, `TestCustomAuth`; `pkg/proxy/backend/backend_conn_mgr_test.go:TestConnAttrs`, `TestCustomHandshake`; `rust/crates/dataplane/tests/session_engine.rs:compressed_client_zstd_roundtrips_end_to_end` (the client zstd level is carried into the auth relay and the backend handshake-forward only carries the level when the backend leg negotiated ZSTD — Go parity) | `session-core`, `control-proto` | `PARITY-HS-006` | GO-VERIFIED / RUST-PARTIAL (WIRE-C: user/database/collation and the per-leg zstd level are preserved across backend auth; the handshake-hook user/attribute rewrite + router-select path remains SES/MIG scope) | P0 | P0 |
| HS-007 | Retry backend selection only when the handshake hook explicitly classifies a TiDB error as retryable; close and unregister each abandoned routed backend. | `pkg/proxy/backend/authenticator.go` (`RECONNECT`); `pkg/proxy/backend/backend_conn_mgr.go:abandonRoutedBackend` | `pkg/proxy/backend/backend_conn_mgr_test.go:TestCustomHandshake`, `TestGetBackendIO` | `session-core`, `control-proto` | `PARITY-HS-007` | GO-VERIFIED / RUST-TODO | P1 | P0 |
| HS-008 | Translate handshake failures into the same client-visible MySQL errors and error-source labels, including capability, TLS, PROXY-v2, authentication, routing, and timeout failures. | `pkg/proxy/backend/error.go`; `pkg/proxy/backend/backend_conn_mgr.go:Connect`; `pkg/proxy/backend/authenticator.go:handleHandshakeError` | `pkg/proxy/backend/authenticator_test.go:TestUnsupportedCapability`, `TestRequireBackendTLS`, `TestProxyProtocol`, `TestAuthFail`; `pkg/proxy/backend/backend_conn_mgr_test.go:TestConnectFail` | `session-core` | `PARITY-HS-008` | GO-VERIFIED / RUST-TODO | P1 | P0 |
| HS-009 | Support direct backend authentication for replay/test clients with generated native or caching-SHA auth responses and optional initial database. This path remains test/topology support, not traffic capture. | `pkg/proxy/backend/authenticator.go:handshakeWithBackend` | `pkg/proxy/backend/authenticator_test.go:TestHandshakeWithBackend`; `pkg/proxy/backend/backend_conn_mgr_test.go:TestConnectWithBackend` | `session-core` | `PARITY-HS-009` | GO-VERIFIED / RUST-TODO | P2 | P2 |
| HS-010 | Advertise the exact Go capability set: LONG_PASSWORD, FOUND_ROWS, LONG_FLAG, CONNECT_WITH_DB, ODBC, LOCAL_FILES, INTERACTIVE, PROTOCOL_41, SSL when configured, TRANSACTIONS, RESERVED, SECURE_CONNECTION, MULTI_STATEMENTS, MULTI_RESULTS, PLUGIN_AUTH, CONNECT_ATTRS, PLUGIN_AUTH_LENENC_CLIENT_DATA, DEPRECATE_EOF, COMPRESS, and ZSTD. Unsupported client flags are logged and masked. | `pkg/proxy/backend/authenticator.go:SupportedServerCapabilities` | `pkg/proxy/backend/authenticator_test.go:TestUnsupportedCapability`, `TestCapability`, `TestCompressProtocol`; `rust/crates/dataplane/tests/session_engine.rs:compressed_client_{zlib,zstd}_roundtrips_end_to_end` (COMPRESS + ZSTD are advertised and negotiate); `rust/crates/tiproxy-rs/src/main.rs:integration_capabilities_are_the_honest_contract` | `session-core` | `PARITY-HS-010` | GO-VERIFIED / RUST-IMPLEMENTED (WIRE-C: `proxy_capability_base` now advertises COMPRESS + ZSTD alongside the full set; unsupported client flags remain masked) | P0 | P0 |
| HS-011 | Parse the backend greeting's version, connection ID, salt, capability halves, status, collation, and auth plugin before capability/TLS decisions; malformed/error greetings must not panic or allocate unbounded memory. | `pkg/proxy/net/mysql.go:ParseInitialHandshake`; `pkg/proxy/backend/authenticator.go:readInitialHandshake` | `pkg/proxy/net/mysql_test.go:TestHandshakeResp`; negative backend-greeting coverage is missing | `mysql-wire`, `session-core` | `PARITY-HS-011` | GO-IMPLEMENTED / RUST-TODO | P0 | P0 |

## Shared command-response state machines

| ID | Required behavior and invariant | Go source | Go test evidence | Rust owner | Target test ID | Status | Canary | Cutover |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| RSP-001 | Forward single OK, ERR, EOF, or raw-string responses, flush at the same boundary, preserve MySQL errors as nonfatal command results, and reject impossible response headers. | `pkg/proxy/backend/cmd_processor_exec.go:forwardCommand`, `forwardOnePacket` | `pkg/proxy/backend/cmd_processor_test.go:TestForwardCommands`, `TestQueryError`; `rust/crates/session-core/tests/response_corpus.rs:parity_rsp_001_through_005_and_008_match_go_corpus` (per-packet role and flush-boundary equality against the Go corpus; `tests/dataplane/corpus/v1/manifest.json` maps this row to `query-ok`, `query-error`, `statistics`) | `session-core` | `PARITY-RSP-001` | GO-VERIFIED / RUST-VERIFIED (observer implemented and corpus-compared; no code change was needed -- the prior RUST-TODO was a stale label) | P0 | P0 |
| RSP-002 | In classic EOF mode, terminate column definitions and rows on short EOF packets, update transaction/cursor flags from the server status, and forward errors occurring while rows stream. | `pkg/proxy/backend/cmd_processor_exec.go:forwardUntilResultEnd`, `forwardResultSet`; `pkg/proxy/net/mysql.go:IsEOFPacket` | `pkg/proxy/backend/cmd_processor_test.go:TestForwardCommands`, `TestDirectQuery`; `rust/crates/session-core/tests/response_corpus.rs:parity_rsp_001_through_005_and_008_match_go_corpus` (per-packet role and flush-boundary equality against the Go corpus; `tests/dataplane/corpus/v1/manifest.json` maps this row to `resultset-legacy-eof`, `field-list`, `process-info`, `stmt-execute-cursor`, `stmt-fetch`); `rust/crates/session-core/src/response.rs:errors_during_row_streaming_complete_the_command_in_both_eof_modes` (an ERR arriving mid-row-stream, which the corpus `query-error` case and the QueryStart ERR test do not reach) | `session-core`, `mysql-wire` | `PARITY-RSP-002` | GO-VERIFIED / RUST-VERIFIED (classic-EOF termination and status updates corpus-compared, and the row-stream ERR clause pinned separately in both EOF modes; stale label, no code change) | P0 | P0 |
| RSP-003 | With `CLIENT_DEPRECATE_EOF`, recognize resultset terminators as `0xfe` OK packets of protocol-41 length, update status, and do not expect metadata EOF packets. | `pkg/proxy/backend/cmd_processor_exec.go`; `pkg/proxy/net/mysql.go:IsResultSetOKPacket` | `pkg/proxy/backend/cmd_processor_test.go:TestForwardCommands`, `TestDirectQuery`; `pkg/proxy/backend/authenticator_test.go:TestUnsupportedCapability`; `rust/crates/session-core/tests/response_corpus.rs:parity_rsp_001_through_005_and_008_match_go_corpus` (per-packet role and flush-boundary equality against the Go corpus; `tests/dataplane/corpus/v1/manifest.json` maps this row to `resultset-deprecate-eof`); `rust/crates/dataplane/tests/session_engine.rs` negotiates `CLIENT_DEPRECATE_EOF` on the production path | `session-core`, `mysql-wire` | `PARITY-RSP-003` | GO-VERIFIED / RUST-VERIFIED (`0xfe` recognised as the protocol-41-length resultset OK under `CLIENT_DEPRECATE_EOF`; stale label, no code change) | P0 | P0 |
| RSP-004 | Continue result processing while `SERVER_MORE_RESULTS_EXISTS` is set for multi-statements and prepared-statement multi-results; stop immediately on ERR because later statements are not executed. | `pkg/proxy/backend/cmd_processor_exec.go:forwardQueryCmd`; `pkg/proxy/net/server_status.go` | `pkg/proxy/backend/cmd_processor_test.go:TestMultiStmt`, `TestQueryError`, `TestForwardCommands`; `rust/crates/session-core/tests/response_corpus.rs:parity_rsp_001_through_005_and_008_match_go_corpus` (per-packet role and flush-boundary equality against the Go corpus; `tests/dataplane/corpus/v1/manifest.json` maps this row to `query-multi-results`); `rust/crates/dataplane/tests/session_engine.rs:multi_result_command_forwards_every_set_and_keeps_the_session_synchronised` drives a second result set through the engine's own `response_rounds` loop, which the corpus cannot reach -- the corpus proves the observer classifies `SERVER_MORE_RESULTS_EXISTS`, this proves the engine acts on it and stays in sequence afterwards | `session-core` | `PARITY-RSP-004` | GO-VERIFIED / RUST-VERIFIED (continuation held at BOTH layers: the observer's classification by corpus, and the engine's `MoreResults` arm by a dataplane test whose negative control -- returning as if complete -- fails it. That engine-level evidence was the one real gap in this family; the other five rows were stale labels) | P0 | P0 |
| RSP-005 | For LOCAL INFILE, flush the server request to the client, stream client file packets until the empty terminator (including a header-only packet), flush to TiDB, then forward final OK/ERR and status. | `pkg/proxy/backend/cmd_processor_exec.go:forwardLoadInFile` | `pkg/proxy/backend/cmd_processor_test.go:TestForwardCommands`; `pkg/proxy/net/packetio_test.go:TestReadPacketEmptyPayload`; `rust/crates/session-core/tests/response_corpus.rs:parity_rsp_001_through_005_and_008_match_go_corpus` (per-packet role and flush-boundary equality against the Go corpus; `tests/dataplane/corpus/v1/manifest.json` maps this row to `local-infile`); `rust/crates/session-core/tests/special_flows.rs:local_infile_end_to_end`, `empty_file_terminates_immediately` | `session-core`, `mysql-wire` | `PARITY-RSP-005` | GO-VERIFIED / RUST-VERIFIED (LOCAL INFILE streaming incl. the header-only empty terminator; stale label, no code change) | P0 | P0 |
| RSP-006 | For COM_STMT_PREPARE OK, extract the statement ID, forward exactly the declared parameter/column definitions plus classic EOF packets (omit both EOF packets under `CLIENT_DEPRECATE_EOF`), then flush once; an ERR ends immediately. | `pkg/proxy/backend/cmd_processor_exec.go:forwardPrepareCmd` | `pkg/proxy/backend/cmd_processor_test.go:TestForwardCommands`, `TestPreparedStmts`; `pkg/proxy/net/mysql_test.go:TestPrepareStmts` | `session-core`, `mysql-wire` | `PARITY-RSP-006` | GO-VERIFIED / RUST-IMPLEMENTED | P0 | P0 |
| RSP-007 | Parse internally issued resultsets for session-state migration in both EOF modes, including column names, rows, status, and row-stream errors; reject LOCAL INFILE and malformed resultsets. | `pkg/proxy/backend/cmd_processor_query.go` | `pkg/proxy/backend/cmd_processor_test.go:TestDirectQuery`, `TestNetworkError`; `rust/crates/dataplane/tests/session_engine.rs:redirect_restores_candidate_and_swaps_atomically`, `complete_snapshot_validation_failures_preserve_old_backend`, `incomplete_snapshot_failures_close_the_session` | `session-core`, `mysql-wire`, `dataplane` | `PARITY-RSP-007` | GO-VERIFIED / RUST-VERIFIED (restricted parser bound to the production old-backend socket at the FSM safe boundary) | P0 | P0 |
| RSP-008 | Preserve status transitions from OK/EOF: transaction open/closed, cursor exists/last row, more results, and reset/change-user clearing prepared state. | `pkg/proxy/backend/cmd_processor.go` | `pkg/proxy/backend/cmd_processor_test.go:TestTxnStatus`, `TestPreparedStmts`, `TestMixPrepAndTxnStatus`, `TestMultiStmt`; `rust/crates/session-core/tests/response_corpus.rs:parity_rsp_001_through_005_and_008_match_go_corpus` (per-packet role and flush-boundary equality against the Go corpus; `tests/dataplane/corpus/v1/manifest.json` maps this row to 8 cases incl. `query-ok`, `field-list`, `process-info`, `resultset-deprecate-eof`); `rust/crates/session-core/tests/prepared_lifecycle.rs` covers reset/change-user clearing prepared state | `session-core` | `PARITY-RSP-008` | GO-VERIFIED / RUST-VERIFIED (transaction/cursor/more-results status transitions and reset/change-user prepared-state clearing; stale label, no code change) | P0 | P0 |

## MySQL command dispatch

Every command value declared by Go has a stable row. `Response contract` is the
branch exercised by `TestForwardCommands`; all normal rows also inherit
RSP-001 through RSP-008 where applicable.

| ID | Byte / command | Response contract and state effect | Go source / test | Rust owner | Target test ID | Status | Canary | Cutover |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| CMD-000 | `0x00 COM_SLEEP` | Generic ERR response. | `pkg/proxy/net/command.go`; `pkg/proxy/backend/cmd_processor_test.go:TestForwardCommands`; `rust/crates/session-core/tests/command_corpus.rs:parity_cmd_000_through_031_dispatch_from_go_corpus` (case `command-sleep`, replayed from the generated Go corpus bytes); `rust/crates/session-core/src/response.rs:generic_one_packet_accepts_shared_ok_error_and_eof_contract` | `session-core` | `PARITY-CMD-000` | GO-VERIFIED / RUST-VERIFIED (dispatch is replayed from the generated Go corpus bytes, and the corpus test also asserts `plan.command.as_byte() == index` so the command cannot be decoded as a neighbouring one. The generic response contract -- OK, ERR, and `0xfe` under either EOF mode, each completing at a protocol boundary -- is held separately. Production runs the same `session_core::command::dispatch`, called at `dataplane/src/session_engine.rs:2529`) | P2 | P2 |
| CMD-001 | `0x01 COM_QUIT` | Forward request, expect no response, mark quit, and close normally. | `pkg/proxy/backend/cmd_processor_exec.go:forwardQuitCmd`; `pkg/proxy/backend/cmd_processor_test.go:TestForwardCommands`; `rust/crates/session-core/tests/command_corpus.rs:parity_cmd_000_through_031_dispatch_from_go_corpus` (case `quit`, replayed from the generated Go corpus bytes); `rust/crates/session-core/src/command.rs:state_changes_apply_only_at_declared_boundary`; `rust/crates/dataplane/tests/session_engine.rs:session_path_emits_query_traffic_and_exact_quit_source` | `session-core` | `PARITY-CMD-001` | GO-VERIFIED / RUST-VERIFIED (the corpus case asserts the trace carries **no** backend packet at all, which is what `ExpectedResponse::None` means here. `MarkQuit` is emitted at the `after_forward` boundary rather than `after_success` -- there is no success to wait for -- and `state.quit()` is asserted after applying it. Production raises `SessionEvent::ClientCommandQuit` at `session_engine.rs:2540` only for this command, and the live session path closes with `QuitSource::None`, i.e. no error source attributed to a clean quit. Production runs the same `session_core::command::dispatch`, called at `dataplane/src/session_engine.rs:2529`) | P1 | P1 |
| CMD-002 | `0x02 COM_INIT_DB` | Generic OK/ERR; the current database is recovered from session state before a later migration. | `pkg/proxy/backend/cmd_processor_exec.go:forwardCommand`; `pkg/proxy/backend/cmd_processor_test.go:TestForwardCommands`; `pkg/proxy/backend/authenticator_test.go:TestSecondHandshake`; `rust/crates/session-core/tests/command_corpus.rs:parity_cmd_000_through_031_dispatch_from_go_corpus`, `corpus_drives_command_state_effects` (dispatch and state effects replayed from the Go corpus; production uses the same `session_core::command::dispatch`, imported at `dataplane/src/session_engine.rs:109`); `tests/dataplane/corpus/v1/manifest.json` case `init-db`; production applies the resulting mutation through `dataplane/src/session_engine.rs:apply_command_mutations`, and `rust/crates/dataplane/tests/session_engine.rs:redirect_restores_candidate_and_swaps_atomically` shows the snapshot's current database carried into the second handshake (the later-migration clause, which dispatch evidence alone does not cover) | `session-core` | `PARITY-CMD-002` | GO-VERIFIED / RUST-VERIFIED (corpus-driven dispatch; stale label, no code change) | P0 | P0 |
| CMD-003 | `0x03 COM_QUERY` | OK/ERR/resultset/LOCAL INFILE, multi-results, transaction tracking, and large-packet streaming. | `pkg/proxy/backend/cmd_processor_exec.go:forwardQueryCmd`; `pkg/proxy/backend/cmd_processor_test.go:TestForwardCommands`, `TestMultiStmt`, `TestTxnStatus`; `rust/crates/session-core/tests/command_corpus.rs:parity_cmd_000_through_031_dispatch_from_go_corpus` replays COM_QUERY dispatch from the Go corpus (the `query-ok` case); production uses the same `session_core::command::dispatch` (`dataplane/src/session_engine.rs:109`); `rust/crates/dataplane/tests/session_engine.rs:oversized_command_streams_to_the_backend_without_being_materialized` (the large-packet clause, driven as a COM_QUERY), `multi_result_command_forwards_every_set_and_keeps_the_session_synchronised`, `drain_waits_for_open_transaction_commit`; `rust/crates/session-core/tests/special_flows.rs:local_infile_end_to_end` | `session-core` | `PARITY-CMD-003` | GO-VERIFIED / RUST-VERIFIED (**this row was partial only on the request-leg streaming clause, and that follow-up has now landed in the same change as PKT-003** -- the streaming test is itself a COM_QUERY larger than the engine's materialization ceiling, so it proves this row's clause directly rather than by adjacency. The rest were already held: OK/ERR/resultset shapes by the RSP-001/002/003 corpus cases, LOCAL INFILE by RSP-005's corpus case and the `special_flows` end-to-end, multi-results by the engine test that drives a second result set through `response_rounds`, and transaction tracking by the observer's status handling plus the drain that waits for an open transaction to commit) | P0 | P0 |
| CMD-004 | `0x04 COM_FIELD_LIST` | Stream column definitions until EOF/ERR in both EOF modes. | `pkg/proxy/backend/cmd_processor_exec.go:forwardFieldListCmd`; `pkg/proxy/backend/cmd_processor_test.go:TestForwardCommands`; `rust/crates/session-core/tests/command_corpus.rs:parity_cmd_000_through_031_dispatch_from_go_corpus` (case `field-list`, replayed from the generated Go corpus bytes); `rust/crates/session-core/tests/response_corpus.rs:parity_rsp_001_through_005_and_008_match_go_corpus` (case `field-list`); `rust/crates/session-core/src/response.rs:marker_meanings_are_contextual`, `field_list_and_fetch_apply_their_terminal_status` | `session-core` | `PARITY-CMD-004` | GO-VERIFIED / RUST-VERIFIED (both EOF modes are driven for the streaming half: under `LEGACY_CAPS` and `MODERN_CAPS` alike the markers `0x00`, `0x01`, `0xfb` and a `0xfe`-prefixed packet all stay `ColumnDefinition`/`Continue` rather than terminating, so nothing in the column stream is mistaken for the end. The termination half is held by the real Go trace -- `ColumnDefinition` then `Terminator`, `CompleteSuccess`, `AUTOCOMMIT`, exactly one flush boundary -- and by the terminal-status test. Production runs the same `session_core::command::dispatch`, called at `dataplane/src/session_engine.rs:2529`) | P1 | P1 |
| CMD-005 | `0x05 COM_CREATE_DB` | Generic OK/ERR and status update. | `pkg/proxy/backend/cmd_processor_exec.go:forwardCommand`; `pkg/proxy/backend/cmd_processor_test.go:TestForwardCommands`; `rust/crates/session-core/tests/command_corpus.rs:parity_cmd_000_through_031_dispatch_from_go_corpus` (case `command-create-db`, replayed from the generated Go corpus bytes); `rust/crates/session-core/src/response.rs:generic_one_packet_accepts_shared_ok_error_and_eof_contract` | `session-core` | `PARITY-CMD-005` | GO-VERIFIED / RUST-VERIFIED (dispatch is replayed from the generated Go corpus bytes, and the corpus test also asserts `plan.command.as_byte() == index` so the command cannot be decoded as a neighbouring one. The generic response contract -- OK, ERR, and `0xfe` under either EOF mode, each completing at a protocol boundary -- is held separately. Production runs the same `session_core::command::dispatch`, called at `dataplane/src/session_engine.rs:2529`) | P1 | P1 |
| CMD-006 | `0x06 COM_DROP_DB` | Generic OK/ERR and status update. | `pkg/proxy/backend/cmd_processor_exec.go:forwardCommand`; `pkg/proxy/backend/cmd_processor_test.go:TestForwardCommands`; `rust/crates/session-core/tests/command_corpus.rs:parity_cmd_000_through_031_dispatch_from_go_corpus` (case `command-drop-db`, replayed from the generated Go corpus bytes); `rust/crates/session-core/src/response.rs:generic_one_packet_accepts_shared_ok_error_and_eof_contract` | `session-core` | `PARITY-CMD-006` | GO-VERIFIED / RUST-VERIFIED (dispatch is replayed from the generated Go corpus bytes, and the corpus test also asserts `plan.command.as_byte() == index` so the command cannot be decoded as a neighbouring one. The generic response contract -- OK, ERR, and `0xfe` under either EOF mode, each completing at a protocol boundary -- is held separately. Production runs the same `session_core::command::dispatch`, called at `dataplane/src/session_engine.rs:2529`) | P1 | P1 |
| CMD-007 | `0x07 COM_REFRESH` | Generic OK/ERR and status update. | `pkg/proxy/backend/cmd_processor_exec.go:forwardCommand`; `pkg/proxy/backend/cmd_processor_test.go:TestForwardCommands`; `rust/crates/session-core/tests/command_corpus.rs:parity_cmd_000_through_031_dispatch_from_go_corpus` (case `command-refresh`, replayed from the generated Go corpus bytes); `rust/crates/session-core/src/response.rs:generic_one_packet_accepts_shared_ok_error_and_eof_contract` | `session-core` | `PARITY-CMD-007` | GO-VERIFIED / RUST-VERIFIED (dispatch is replayed from the generated Go corpus bytes, and the corpus test also asserts `plan.command.as_byte() == index` so the command cannot be decoded as a neighbouring one. The generic response contract -- OK, ERR, and `0xfe` under either EOF mode, each completing at a protocol boundary -- is held separately. Production runs the same `session_core::command::dispatch`, called at `dataplane/src/session_engine.rs:2529`) | P2 | P2 |
| CMD-008 | `0x08 COM_SHUTDOWN` (deprecated slot) | Generic OK/ERR. It must not be reinterpreted as a new command. | `pkg/proxy/backend/cmd_processor_exec.go:forwardCommand`; `pkg/proxy/backend/cmd_processor_test.go:TestForwardCommands`; `rust/crates/session-core/tests/command_corpus.rs:parity_cmd_000_through_031_dispatch_from_go_corpus` (case `command-deprecated-shutdown`, replayed from the generated Go corpus bytes); `rust/crates/session-core/src/response.rs:generic_one_packet_accepts_shared_ok_error_and_eof_contract` | `session-core` | `PARITY-CMD-008` | GO-VERIFIED / RUST-VERIFIED (the row's real requirement is the negative one, and the corpus test holds it structurally: it asserts `plan.command.as_byte() == index` for all 32 entries, so `0x08` can only decode as `DeprecatedShutdown`. Reusing the slot for a new command would move some other command's byte and fail that identity. Production runs the same `session_core::command::dispatch`, called at `dataplane/src/session_engine.rs:2529`) | P2 | P2 |
| CMD-009 | `0x09 COM_STATISTICS` | Forward exactly one raw string packet. | `pkg/proxy/backend/cmd_processor_exec.go:forwardStatisticsCmd`; `pkg/proxy/backend/cmd_processor_test.go:TestForwardCommands`; `rust/crates/session-core/tests/command_corpus.rs:parity_cmd_000_through_031_dispatch_from_go_corpus` (case `statistics`, replayed from the generated Go corpus bytes); `rust/crates/session-core/tests/response_corpus.rs:parity_rsp_001_through_005_and_008_match_go_corpus` (case `statistics`) | `session-core` | `PARITY-CMD-009` | GO-VERIFIED / RUST-VERIFIED (exactly one packet, role `Raw`, disposition `CompleteRaw`, flushed at packet 0 -- the string is forwarded verbatim without being parsed as OK/ERR/EOF. Production runs the same `session_core::command::dispatch`, called at `dataplane/src/session_engine.rs:2529`) | P2 | P2 |
| CMD-010 | `0x0a COM_PROCESS_INFO` | ERR or resultset using query response state. | `pkg/proxy/backend/cmd_processor_exec.go:forwardQueryCmd`; `pkg/proxy/backend/cmd_processor_test.go:TestForwardCommands`; `rust/crates/session-core/tests/command_corpus.rs:parity_cmd_000_through_031_dispatch_from_go_corpus` (case `process-info`, replayed from the generated Go corpus bytes); `rust/crates/session-core/tests/response_corpus.rs:parity_rsp_001_through_005_and_008_match_go_corpus` (case `process-info`) | `session-core` | `PARITY-CMD-010` | GO-VERIFIED / RUST-VERIFIED (dispatch selects `ExpectedResponse::Query`, the same state COM_QUERY uses, and the real Go trace walks the full resultset shape through it: header, column definition, metadata terminator, row, row terminator, with the single flush boundary at the last packet. Production runs the same `session_core::command::dispatch`, called at `dataplane/src/session_engine.rs:2529`) | P1 | P1 |
| CMD-011 | `0x0b COM_CONNECT` | Generic ERR response. | `pkg/proxy/backend/cmd_processor_exec.go:forwardCommand`; `pkg/proxy/backend/cmd_processor_test.go:TestForwardCommands`; `rust/crates/session-core/tests/command_corpus.rs:parity_cmd_000_through_031_dispatch_from_go_corpus` (case `command-connect`, replayed from the generated Go corpus bytes); `rust/crates/session-core/src/response.rs:generic_one_packet_accepts_shared_ok_error_and_eof_contract` | `session-core` | `PARITY-CMD-011` | GO-VERIFIED / RUST-VERIFIED (dispatch is replayed from the generated Go corpus bytes, and the corpus test also asserts `plan.command.as_byte() == index` so the command cannot be decoded as a neighbouring one. The generic response contract -- OK, ERR, and `0xfe` under either EOF mode, each completing at a protocol boundary -- is held separately. Production runs the same `session_core::command::dispatch`, called at `dataplane/src/session_engine.rs:2529`) | P2 | P2 |
| CMD-012 | `0x0c COM_PROCESS_KILL` | Generic OK/ERR and status update. | `pkg/proxy/backend/cmd_processor_exec.go:forwardCommand`; `pkg/proxy/backend/cmd_processor_test.go:TestForwardCommands`; `rust/crates/session-core/tests/command_corpus.rs:parity_cmd_000_through_031_dispatch_from_go_corpus` (case `command-process-kill`, replayed from the generated Go corpus bytes); `rust/crates/session-core/src/response.rs:generic_one_packet_accepts_shared_ok_error_and_eof_contract` | `session-core` | `PARITY-CMD-012` | GO-VERIFIED / RUST-VERIFIED (dispatch is replayed from the generated Go corpus bytes, and the corpus test also asserts `plan.command.as_byte() == index` so the command cannot be decoded as a neighbouring one. The generic response contract -- OK, ERR, and `0xfe` under either EOF mode, each completing at a protocol boundary -- is held separately. Production runs the same `session_core::command::dispatch`, called at `dataplane/src/session_engine.rs:2529`) | P1 | P1 |
| CMD-013 | `0x0d COM_DEBUG` | Generic EOF/ERR; interpret `0xfe` according to negotiated EOF mode. | `pkg/proxy/backend/cmd_processor_exec.go:forwardCommand`; `pkg/proxy/backend/cmd_processor_test.go:TestForwardCommands`; `rust/crates/session-core/tests/command_corpus.rs:parity_cmd_000_through_031_dispatch_from_go_corpus` (case `command-debug`, replayed from the generated Go corpus bytes); `rust/crates/session-core/src/response.rs:generic_one_packet_accepts_shared_ok_error_and_eof_contract` | `session-core` | `PARITY-CMD-013` | GO-VERIFIED / RUST-VERIFIED (the `0xfe` clause is the whole row and it is driven on both sides of the capability branch: under `LEGACY_CAPS` a short EOF packet parses through `parse_legacy_eof_status`, and under `MODERN_CAPS` the same first byte is read as a protocol-41-length OK through `parse_ok_status`, both completing as `Terminator`. Production runs the same `session_core::command::dispatch`, called at `dataplane/src/session_engine.rs:2529`) | P2 | P2 |
| CMD-014 | `0x0e COM_PING` | Generic OK and status update. | `pkg/proxy/backend/cmd_processor_exec.go:forwardCommand`; `pkg/proxy/backend/cmd_processor_test.go:TestForwardCommands`; `rust/crates/session-core/tests/command_corpus.rs:parity_cmd_000_through_031_dispatch_from_go_corpus`, `corpus_drives_command_state_effects` (dispatch and state effects replayed from the Go corpus; production uses the same `session_core::command::dispatch`, imported at `dataplane/src/session_engine.rs:109`); `tests/dataplane/corpus/v1/manifest.json` case `ping`; `rust/crates/session-core/tests/response_corpus.rs` `ping` case asserts the OK/status effect, which command dispatch alone does not cover | `session-core` | `PARITY-CMD-014` | GO-VERIFIED / RUST-VERIFIED (corpus-driven dispatch; stale label, no code change) | P0 | P1 |
| CMD-015 | `0x0f COM_TIME` | Generic ERR response. | `pkg/proxy/backend/cmd_processor_exec.go:forwardCommand`; `pkg/proxy/backend/cmd_processor_test.go:TestForwardCommands`; `rust/crates/session-core/tests/command_corpus.rs:parity_cmd_000_through_031_dispatch_from_go_corpus` (case `command-time`, replayed from the generated Go corpus bytes); `rust/crates/session-core/src/response.rs:generic_one_packet_accepts_shared_ok_error_and_eof_contract` | `session-core` | `PARITY-CMD-015` | GO-VERIFIED / RUST-VERIFIED (dispatch is replayed from the generated Go corpus bytes, and the corpus test also asserts `plan.command.as_byte() == index` so the command cannot be decoded as a neighbouring one. The generic response contract -- OK, ERR, and `0xfe` under either EOF mode, each completing at a protocol boundary -- is held separately. Production runs the same `session_core::command::dispatch`, called at `dataplane/src/session_engine.rs:2529`) | P2 | P2 |
| CMD-016 | `0x10 COM_DELAYED_INSERT` | Generic ERR response. | `pkg/proxy/backend/cmd_processor_exec.go:forwardCommand`; `pkg/proxy/backend/cmd_processor_test.go:TestForwardCommands`; `rust/crates/session-core/tests/command_corpus.rs:parity_cmd_000_through_031_dispatch_from_go_corpus` (case `command-delayed-insert`, replayed from the generated Go corpus bytes); `rust/crates/session-core/src/response.rs:generic_one_packet_accepts_shared_ok_error_and_eof_contract` | `session-core` | `PARITY-CMD-016` | GO-VERIFIED / RUST-VERIFIED (dispatch is replayed from the generated Go corpus bytes, and the corpus test also asserts `plan.command.as_byte() == index` so the command cannot be decoded as a neighbouring one. The generic response contract -- OK, ERR, and `0xfe` under either EOF mode, each completing at a protocol boundary -- is held separately. Production runs the same `session_core::command::dispatch`, called at `dataplane/src/session_engine.rs:2529`) | P2 | P2 |
| CMD-017 | `0x11 COM_CHANGE_USER` | Parse/rewrite request with unknown plugin, perform auth-switch loop, update auth identity on OK, and clear all prepared state. | `pkg/proxy/backend/cmd_processor_exec.go:forwardChangeUserCmd`; `pkg/proxy/backend/authenticator.go:changeUser`; `pkg/proxy/backend/cmd_processor_test.go:TestForwardCommands`; `pkg/proxy/backend/authenticator_test.go:TestSecondHandshake`; `rust/crates/dataplane/tests/session_engine.rs:change_user_success_commits_identity_clears_guard_and_migrates`, `change_user_failure_preserves_identity_and_prepared_guard`, `compressed_zstd_change_user_multi_round_stays_aligned` | `session-core`, `mysql-wire`, `dataplane` | `PARITY-CMD-017` | GO-VERIFIED / RUST-IMPLEMENTED | P0 | P0 |
| CMD-018 | `0x12 COM_BINLOG_DUMP` | Generic ERR response. | `pkg/proxy/backend/cmd_processor_exec.go:forwardCommand`; `pkg/proxy/backend/cmd_processor_test.go:TestForwardCommands`; `rust/crates/session-core/tests/command_corpus.rs:parity_cmd_000_through_031_dispatch_from_go_corpus` (case `command-binlog-dump`, replayed from the generated Go corpus bytes); `rust/crates/session-core/src/response.rs:generic_one_packet_accepts_shared_ok_error_and_eof_contract` | `session-core` | `PARITY-CMD-018` | GO-VERIFIED / RUST-VERIFIED (dispatch is replayed from the generated Go corpus bytes, and the corpus test also asserts `plan.command.as_byte() == index` so the command cannot be decoded as a neighbouring one. The generic response contract -- OK, ERR, and `0xfe` under either EOF mode, each completing at a protocol boundary -- is held separately. Production runs the same `session_core::command::dispatch`, called at `dataplane/src/session_engine.rs:2529`) | P2 | P2 |
| CMD-019 | `0x13 COM_TABLE_DUMP` | Generic ERR response. | `pkg/proxy/backend/cmd_processor_exec.go:forwardCommand`; `pkg/proxy/backend/cmd_processor_test.go:TestForwardCommands`; `rust/crates/session-core/tests/command_corpus.rs:parity_cmd_000_through_031_dispatch_from_go_corpus` (case `command-table-dump`, replayed from the generated Go corpus bytes); `rust/crates/session-core/src/response.rs:generic_one_packet_accepts_shared_ok_error_and_eof_contract` | `session-core` | `PARITY-CMD-019` | GO-VERIFIED / RUST-VERIFIED (dispatch is replayed from the generated Go corpus bytes, and the corpus test also asserts `plan.command.as_byte() == index` so the command cannot be decoded as a neighbouring one. The generic response contract -- OK, ERR, and `0xfe` under either EOF mode, each completing at a protocol boundary -- is held separately. Production runs the same `session_core::command::dispatch`, called at `dataplane/src/session_engine.rs:2529`) | P2 | P2 |
| CMD-020 | `0x14 COM_CONNECT_OUT` | Generic ERR response. | `pkg/proxy/backend/cmd_processor_exec.go:forwardCommand`; `pkg/proxy/backend/cmd_processor_test.go:TestForwardCommands`; `rust/crates/session-core/tests/command_corpus.rs:parity_cmd_000_through_031_dispatch_from_go_corpus` (case `command-connect-out`, replayed from the generated Go corpus bytes); `rust/crates/session-core/src/response.rs:generic_one_packet_accepts_shared_ok_error_and_eof_contract` | `session-core` | `PARITY-CMD-020` | GO-VERIFIED / RUST-VERIFIED (dispatch is replayed from the generated Go corpus bytes, and the corpus test also asserts `plan.command.as_byte() == index` so the command cannot be decoded as a neighbouring one. The generic response contract -- OK, ERR, and `0xfe` under either EOF mode, each completing at a protocol boundary -- is held separately. Production runs the same `session_core::command::dispatch`, called at `dataplane/src/session_engine.rs:2529`) | P2 | P2 |
| CMD-021 | `0x15 COM_REGISTER_SLAVE` | Generic ERR response. | `pkg/proxy/backend/cmd_processor_exec.go:forwardCommand`; `pkg/proxy/backend/cmd_processor_test.go:TestForwardCommands`; `rust/crates/session-core/tests/command_corpus.rs:parity_cmd_000_through_031_dispatch_from_go_corpus` (case `command-register-slave`, replayed from the generated Go corpus bytes); `rust/crates/session-core/src/response.rs:generic_one_packet_accepts_shared_ok_error_and_eof_contract` | `session-core` | `PARITY-CMD-021` | GO-VERIFIED / RUST-VERIFIED (dispatch is replayed from the generated Go corpus bytes, and the corpus test also asserts `plan.command.as_byte() == index` so the command cannot be decoded as a neighbouring one. The generic response contract -- OK, ERR, and `0xfe` under either EOF mode, each completing at a protocol boundary -- is held separately. Production runs the same `session_core::command::dispatch`, called at `dataplane/src/session_engine.rs:2529`) | P2 | P2 |
| CMD-022 | `0x16 COM_STMT_PREPARE` | Use the prepare metadata state machine and capture the returned statement ID for session bookkeeping. | `pkg/proxy/backend/cmd_processor_exec.go:forwardPrepareCmd`; `pkg/proxy/backend/cmd_processor_test.go:TestForwardCommands`, `TestPreparedStmts` | `session-core` | `PARITY-CMD-022` | GO-VERIFIED / RUST-IMPLEMENTED | P0 | P0 |
| CMD-023 | `0x17 COM_STMT_EXECUTE` | OK/ERR/resultset/multi-result response, statement-specific cursor and long-data state. | `pkg/proxy/backend/cmd_processor_exec.go:forwardQueryCmd`; `pkg/proxy/backend/cmd_processor_test.go:TestForwardCommands`, `TestPreparedStmts` | `session-core` | `PARITY-CMD-023` | GO-VERIFIED / RUST-IMPLEMENTED | P0 | P0 |
| CMD-024 | `0x18 COM_STMT_SEND_LONG_DATA` | Forward with no response and block migration for that statement until execute/reset/close. | `pkg/proxy/backend/cmd_processor_exec.go:forwardSendLongDataCmd`; `pkg/proxy/backend/cmd_processor_test.go:TestPreparedStmts` | `session-core` | `PARITY-CMD-024` | GO-VERIFIED / RUST-IMPLEMENTED | P0 | P0 |
| CMD-025 | `0x19 COM_STMT_CLOSE` | Forward with no response and clear only that statement's pending state. | `pkg/proxy/backend/cmd_processor_exec.go:forwardCloseCmd`; `pkg/proxy/backend/cmd_processor_test.go:TestPreparedStmts` | `session-core` | `PARITY-CMD-025` | GO-VERIFIED / RUST-IMPLEMENTED | P0 | P0 |
| CMD-026 | `0x1a COM_STMT_RESET` | Generic OK/ERR; clear pending state only when the response status completes the statement. | `pkg/proxy/backend/cmd_processor.go:updatePrepStmtStatus`; `pkg/proxy/backend/cmd_processor_test.go:TestPreparedStmts`, `TestForwardCommands` | `session-core` | `PARITY-CMD-026` | GO-VERIFIED / RUST-IMPLEMENTED | P0 | P0 |
| CMD-027 | `0x1b COM_SET_OPTION` | EOF/ERR response; value 0 enables and value 1 disables `CLIENT_MULTI_STATEMENTS`; reject any other value as malformed. | `pkg/proxy/backend/backend_conn_mgr.go:ExecuteCmd`; `pkg/proxy/backend/backend_conn_mgr_test.go:TestSpecialCmds`; `pkg/proxy/backend/cmd_processor_test.go:TestForwardCommands`; `rust/crates/session-core/tests/command_corpus.rs:parity_cmd_000_through_031_dispatch_from_go_corpus` (case `set-option`, replayed from the generated Go corpus bytes); `rust/crates/session-core/tests/command_corpus.rs:corpus_drives_command_state_effects`, `parity_cmd_032_and_unknown_policy_are_safe` | `session-core` | `PARITY-CMD-027` | GO-VERIFIED / RUST-VERIFIED (all three clauses come from Go corpus bytes: case `set-option` (value 0) yields `SetMultiStatements(true)`, case `set-option-disable` (value 1) yields `SetMultiStatements(false)`, and case `set-option-malformed` (value 2) is rejected as `CommandDispatchError::InvalidSetOption { value: 2 }` **before forwarding**, not after. Production runs the same `session_core::command::dispatch`, called at `dataplane/src/session_engine.rs:2529`) | P1 | P1 |
| CMD-028 | `0x1c COM_STMT_FETCH` | Stream rows to EOF/OK/ERR and retain cursor state until `SERVER_STATUS_LAST_ROW_SENT`. | `pkg/proxy/backend/cmd_processor_exec.go:forwardFetchCmd`; `pkg/proxy/backend/cmd_processor_test.go:TestForwardCommands`, `TestPreparedStmts` | `session-core` | `PARITY-CMD-028` | GO-VERIFIED / RUST-IMPLEMENTED | P0 | P0 |
| CMD-029 | `0x1d COM_DAEMON` | Generic ERR response. | `pkg/proxy/backend/cmd_processor_exec.go:forwardCommand`; `pkg/proxy/backend/cmd_processor_test.go:TestForwardCommands`; `rust/crates/session-core/tests/command_corpus.rs:parity_cmd_000_through_031_dispatch_from_go_corpus` (case `command-daemon`, replayed from the generated Go corpus bytes); `rust/crates/session-core/src/response.rs:generic_one_packet_accepts_shared_ok_error_and_eof_contract` | `session-core` | `PARITY-CMD-029` | GO-VERIFIED / RUST-VERIFIED (dispatch is replayed from the generated Go corpus bytes, and the corpus test also asserts `plan.command.as_byte() == index` so the command cannot be decoded as a neighbouring one. The generic response contract -- OK, ERR, and `0xfe` under either EOF mode, each completing at a protocol boundary -- is held separately. Production runs the same `session_core::command::dispatch`, called at `dataplane/src/session_engine.rs:2529`) | P2 | P2 |
| CMD-030 | `0x1e COM_BINLOG_DUMP_GTID` | Generic ERR response. | `pkg/proxy/backend/cmd_processor_exec.go:forwardCommand`; `pkg/proxy/backend/cmd_processor_test.go:TestForwardCommands`; `rust/crates/session-core/tests/command_corpus.rs:parity_cmd_000_through_031_dispatch_from_go_corpus` (case `command-binlog-dump-gtid`, replayed from the generated Go corpus bytes); `rust/crates/session-core/src/response.rs:generic_one_packet_accepts_shared_ok_error_and_eof_contract` | `session-core` | `PARITY-CMD-030` | GO-VERIFIED / RUST-VERIFIED (dispatch is replayed from the generated Go corpus bytes, and the corpus test also asserts `plan.command.as_byte() == index` so the command cannot be decoded as a neighbouring one. The generic response contract -- OK, ERR, and `0xfe` under either EOF mode, each completing at a protocol boundary -- is held separately. Production runs the same `session_core::command::dispatch`, called at `dataplane/src/session_engine.rs:2529`) | P2 | P2 |
| CMD-031 | `0x1f COM_RESET_CONNECTION` | Generic OK/ERR and clear all prepared-statement state. | `pkg/proxy/backend/cmd_processor.go:updatePrepStmtStatus`; `pkg/proxy/backend/cmd_processor_test.go:TestForwardCommands`, `TestPreparedStmts` | `session-core` | `PARITY-CMD-031` | GO-VERIFIED / RUST-IMPLEMENTED | P0 | P0 |
| CMD-032 | `0x20 COM_END` sentinel | Not a protocol command. If received, Go currently forwards it via the generic branch and records an ERR test case. Rust must reject it deterministically without indexing metrics out of range. | `pkg/proxy/net/command.go`; `pkg/proxy/backend/cmd_processor_test.go:cmdResponseTypes` | `session-core` | `PARITY-CMD-032` | DECISION-REQUIRED | P1 | P1 |

## Prepared statement and migration safety

| ID | Required behavior and invariant | Go source | Go test evidence | Rust owner | Target test ID | Status | Canary | Cutover |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| PS-001 | Track pending long data independently by statement ID; execute/reset/close affect only the referenced statement. | `pkg/proxy/backend/cmd_processor.go:updatePrepStmtStatus` | `pkg/proxy/backend/cmd_processor_test.go:TestPreparedStmts` | `session-core` | `PARITY-PS-001` | GO-VERIFIED / RUST-IMPLEMENTED | P0 | P0 |
| PS-002 | Track open cursors after execute and subsequent fetches until last-row status, including multiple simultaneous statement IDs. | `pkg/proxy/backend/cmd_processor.go:updatePrepStmtStatus` | `pkg/proxy/backend/cmd_processor_test.go:TestPreparedStmts`, `TestMixPrepAndTxnStatus` | `session-core` | `PARITY-PS-002` | GO-VERIFIED / RUST-IMPLEMENTED | P0 | P0 |
| PS-003 | Treat execute ERR after long data as still pending (TiDB may have consumed an incomplete stream) and prohibit migration until an explicit reset/close/user reset. | `pkg/proxy/backend/cmd_processor.go:updatePrepStmtStatus` | `pkg/proxy/backend/cmd_processor_test.go:TestPreparedStmts` | `session-core` | `PARITY-PS-003` | GO-VERIFIED / RUST-IMPLEMENTED | P0 | P0 |
| PS-004 | Parse and encode COM_STMT_PREPARE/EXECUTE/CLOSE, null bitmap, signed/unsigned numeric types, temporal/string/blob/json/vector values, and reuse prior parameter types when the new-types flag is false. | `pkg/proxy/net/mysql.go` | `pkg/proxy/net/mysql_test.go:TestPrepareStmts`; `rust/crates/session-core/tests/prepared_corpus.rs` asserts PS-001..PS-006 against the Go corpus for signed/unsigned, string, null and type reuse (`stmt-execute-cursor`, `stmt-execute-types-reuse`, `stmt-prepare-metadata`, `stmt-prepare-metadata-deprecate-eof`); temporal, blob, json and vector parameter encodings are covered by `rust/crates/mysql-wire/src/prepared.rs:prepare_and_statement_commands_round_trip`, `execute_types_values_and_reuse_round_trip`, `temporal_blob_json_vector_and_truncation_are_safe`, with per-statement retention in `rust/crates/session-core/tests/prepared_lifecycle.rs:execute_codec_types_and_reuse_are_retained_per_statement` | `mysql-wire` | `PARITY-PS-004` | GO-VERIFIED / RUST-VERIFIED (corpus covers signed/unsigned, string, null and type reuse; the remaining temporal/blob/json/vector encodings rest on the mysql-wire unit tests cited beside it, not on the corpus cases -- stale label, no code change) | P0 | P0 |
| PS-005 | Clear every prepared-state guard on successful COM_CHANGE_USER or COM_RESET_CONNECTION. | `pkg/proxy/backend/cmd_processor.go:updatePrepStmtStatus` | `pkg/proxy/backend/cmd_processor_test.go:TestPreparedStmts`; `rust/crates/dataplane/tests/session_engine.rs:change_user_success_commits_identity_clears_guard_and_migrates`, `change_user_failure_preserves_identity_and_prepared_guard` | `session-core`, `dataplane` | `PARITY-PS-005` | GO-VERIFIED / RUST-IMPLEMENTED | P0 | P0 |
| PS-006 | A session is redirectable only when it is outside a transaction, has not quit, and has no pending long-data/cursor state. | `pkg/proxy/backend/cmd_processor.go:finishedTxn` | `pkg/proxy/backend/cmd_processor_test.go:TestPreparedStmts`, `TestTxnStatus`, `TestMixPrepAndTxnStatus` | `session-core` | `PARITY-PS-006` | GO-VERIFIED / RUST-VERIFIED (**this row was partial only because its note listed covered evidence and never named a residual; auditing the requirement clause by clause found none left.** All four hold. *Outside a transaction*: a `ControlRedirect` after `BackendResponseTxnOpen` is rejected outright (`rust/crates/session-core/tests/fsm_model.rs:redirect_failure_keeps_owner_walk`), and end to end the drain waits for the commit (`drain_waits_for_open_transaction_commit`). *Has not quit*: quit applies `MarkQuit` at the after-forward boundary and moves the FSM to Closing, so redirect is excluded structurally rather than by a flag test -- `fsm_model.rs:exhaustive_model_check` walks every reachable state against every event, so no post-quit redirect path escapes it. *No pending long data*: `prepared_guard_blocks_redirect_and_drain` is exact -- under `PreparedStatePending` a `ControlRedirect` leaves the session in `Ready` and emits **no** `StartRedirectHandshake`, and only after `PreparedStateClear` does the next completion boundary start it; `drain_waits_for_prepared_long_data_guard` shows the same for drain. *No cursor state*: `rust/crates/session-core/tests/prepared_lifecycle.rs:independent_guards_preserve_execute_and_fetch_errors` derives `CursorOpen` from `CURSOR_EXISTS`, keeps it across a fetch error, and holds `has_pending()` true -- and `has_pending` is what feeds the SES-00 event the FSM gates on, so the cursor clause reaches the same gate as the long-data one) | P0 | P0 |
| MIG-001 | Accept nonblocking redirect signals, serialize migration against commands, defer while the session is unsafe, and deliver exactly one asynchronous success/failure notification. | `pkg/proxy/backend/backend_conn_mgr.go:Redirect`, `processSignals`, `tryRedirect`, `notifyRedirectResult` | `pkg/proxy/backend/backend_conn_mgr_test.go:TestNormalRedirect`, `TestRedirectInTxn`, `TestRedirectFail`, `TestCloseWhileRedirect`, `TestRedirectAbortedByCloseReportsFail`; `rust/crates/dataplane/tests/session_engine.rs:redirect_restores_candidate_and_swaps_atomically`, `candidate_failures_preserve_old_backend` (the Go framed-UDS adapter test that also covered this was deleted at #223 Phase 2 with `router_adapter.go`; the Rust evidence above is unaffected) | `dataplane`, `control-proto`, `session-core` | `PARITY-MIG-001` | GO-VERIFIED / RUST-VERIFIED (exact target/id/generation share the owner FIFO; stale generations stop before target I/O, completed duplicates replay without a second dial/snapshot/restore, FSM serialization/deferral prevents dual command ownership, and the real framed UDS seam delivered each success/failure callback once — **that seam is old-bridge history**: its Go test went at #223 Phase 2 and no redirect callback crosses the bridge under the route owner. The FSM and duplicate-replay conclusions above stand on their own Rust session-engine evidence) | P0 | P0 |
| MIG-002 | Before reconnect, execute `SHOW SESSION_STATES`; require both session JSON and a nonempty signed session token. | `pkg/proxy/backend/backend_conn_mgr.go:querySessionStates`, `tryRedirect` | `pkg/proxy/backend/backend_conn_mgr_test.go:TestNormalRedirect`, `TestRedirectFail`; `rust/crates/dataplane/tests/session_engine.rs:redirect_restores_candidate_and_swaps_atomically`, `complete_snapshot_validation_failures_preserve_old_backend`, `incomplete_snapshot_failures_close_the_session` | `session-core`, `dataplane` | `PARITY-MIG-002` | GO-VERIFIED / RUST-VERIFIED (bounded parser and production safe-boundary socket binding; complete invalid responses preserve the old backend, disconnect/incomplete oversized responses close) | P0 | P0 |
| MIG-003 | Restore using the `tidb_session_token` auth plugin, negotiate backend TLS/PROXY/compression again, then execute escaped `SET SESSION_STATES` before swapping sockets. | `pkg/proxy/backend/authenticator.go:handshakeSecondTime`; `pkg/proxy/backend/backend_conn_mgr.go:initSessionStates`, `tryRedirect` | `pkg/proxy/backend/authenticator_test.go:TestSecondHandshake`; `pkg/proxy/backend/backend_conn_mgr_test.go:TestNormalRedirect`; `rust/crates/dataplane/tests/session_engine.rs:redirect_restores_candidate_and_swaps_atomically`, `redirect_restores_candidate_over_backend_tls`, `redirect_restores_candidate_over_zlib`, `redirect_restores_candidate_over_zstd_and_proxy_v2`, `candidate_failures_preserve_old_backend`; `tests/dataplane/integration/run.sh` MIG-01 live same-keyspace phase | `session-core`, `dataplane` | `PARITY-MIG-003` | GO-VERIFIED / RUST-VERIFIED (exact-target plain/TLS/PROXY-v2/zlib/zstd candidate paths, token auth, auth-OK compression activation, escaped restore-OK, atomic swap, and real-TiDB state preservation are exercised) | P0 | P0 |
| MIG-004 | Update the authentication database from `current-db` in session JSON before the second handshake; preserve changed user/database semantics. | `pkg/proxy/backend/backend_conn_mgr.go:updateAuthInfoFromSessionStates`; `pkg/proxy/backend/authenticator.go:updateCurrentDB` | `pkg/proxy/backend/authenticator_test.go:TestSecondHandshake`; `pkg/proxy/backend/backend_conn_mgr_test.go:TestSpecialCmds`; `rust/crates/session-core/src/internal_client.rs:validates_session_json_and_extracts_authoritative_database`; `rust/crates/dataplane/tests/session_engine.rs:redirect_restores_candidate_and_swaps_atomically` | `session-core`, `dataplane` | `PARITY-MIG-004` | GO-VERIFIED / RUST-VERIFIED (the authoritative snapshot database replaces or clears stale local state and is consumed by the second handshake before restore) | P0 | P0 |
| MIG-005 | If redirect is requested during a transaction and the next query begins a new transaction, issue COMMIT on the old backend, migrate, then execute the held request exactly once on the selected backend. Do not hold when a cursor/long-data guard exists. | `pkg/proxy/backend/cmd_processor_exec.go:needHoldRequest`, `executeCmd`; `pkg/proxy/backend/backend_conn_mgr.go:ExecuteCmd` | `pkg/proxy/backend/cmd_processor_test.go:TestHoldRequest`; `pkg/proxy/backend/backend_conn_mgr_test.go:TestRedirectInTxn`; `rust/crates/session-core/tests/boundary_model.rs:held_begin_success_never_forwards_internal_commit`, `held_begin_commit_ok_in_txn_replays_on_current_backend`, `held_begin_commit_error_forwards_exactly_once`, `held_begin_failure_and_close_paths`, `redirect_ready_authorizes_resume_after_swap`, `redirect_failed_still_authorizes_resume`, `held_begin_commit_ok_in_txn_while_draining_drops_without_resume`, `teardown_never_authorizes_resume`; `rust/crates/dataplane/tests/session_engine.rs:redirect_in_transaction_holds_commits_and_replays_begin` | `session-core`, `dataplane` | `PARITY-MIG-005` | GO-VERIFIED / RUST-IMPLEMENTED (the `boundary.rs` hold logic — `need_hold_request` + `HeldBegin` exactly-once — is wired at the command forward point: an armed redirect + BEGIN commits the old backend internally via `InternalQuery::Commit` (its OK never reaches the client), then the FSM `ResumeHeldRequest` effect replays the buffered request exactly once on the new backend after a successful swap, on the old backend after a failed redirect or a commit that left the transaction open, forwards a commit MySQL-ERR verbatim as the BEGIN's answer with no replay, and drops the held request on a graceful close; cursor/long-data guards keep `has_pending()` set so no hold is taken) | P0 | P0 |
| MIG-006 | A failed redirect leaves the old client/backend session usable, except an old-backend disconnect closes the client; do not retry until a new redirect signal. | `pkg/proxy/backend/backend_conn_mgr.go:tryRedirect` | `pkg/proxy/backend/backend_conn_mgr_test.go:TestRedirectFail`, `TestBackendInactive`; `rust/crates/dataplane/tests/session_engine.rs:candidate_failures_preserve_old_backend`, `complete_snapshot_validation_failures_preserve_old_backend`, `expired_redirect_deadline_preserves_old_backend`, `unhealthy_redirect_target_preserves_old_backend`, `incomplete_snapshot_failures_close_the_session` (Go controlbridge evidence deleted at #223 Phase 2) | `dataplane`, `session-core` | `PARITY-MIG-006` | GO-VERIFIED / RUST-VERIFIED (invalid/expired token, unhealthy or expired/unreachable target, restore ERR/disconnect, and complete snapshot failures drop only the candidate; every failed terminal still names the attempted target — **Go closing the matching old-to-target route event is old-bridge history**, since `ConnectionEvent` is a retired tombstone; what remains current is that the physical owner stays old; old-backend disconnect/incomplete response closes the poisoned session) | P0 | P0 |
| MIG-007 | On successful swap, account old traffic, reset counters for the new socket, close old backend, apply health-specific keepalive, update handshake hooks, and retain the stable frontend connection ID. | `pkg/proxy/backend/backend_conn_mgr.go:tryRedirect`, `ConnectionID`, `updateTraffic` | `pkg/proxy/backend/backend_conn_mgr_test.go:TestNormalRedirect`, `TestConnID`, `TestTrafficMetrics`, `TestKeepAlive`; `rust/crates/dataplane/tests/session_engine.rs:redirect_restores_candidate_and_swaps_atomically`, `redirect_restores_candidate_over_zstd_and_proxy_v2`, `existing_session_reapplies_keepalive_on_backend_health_flip` (the two Go controlbridge tests that also covered this were deleted at #223 Phase 2 with `router_adapter.go`; the Rust evidence above is unaffected) | `dataplane` | `PARITY-MIG-007` | GO-VERIFIED / RUST-VERIFIED (atomic single-owner replacement makes the next command new-backend-only; exact retired-plus-current raw-byte accumulation, one callback per terminal, balanced old-to-target pending metrics (**the Go test that proved this balance was deleted at #223 Phase 2; the property is now served natively — see MTR-004 and MTR-006**), healthy-target keepalive, exact success hook, stable frontend connection, and immediate post-swap reconciliation against live topology are exercised) | P1 | P0 |
| MIG-008 | Router-issued dynamic backend redirects are keyspace-stable: the redirect issuance boundary refuses any target whose keyspace differs from the connection's current backend (strict equality; legacy empty==empty allowed; empty vs non-empty fails closed) with one bounded structured record per group cadence (pair-level fast path before the per-connection loop plus a backstop at the final issuance call), and the authoritative path-parsed topology keyspace survives projection in preference to operator labels. | `pkg/balance/router/group.go:Balance`, `redirectConn`, `logCrossKeyspaceSkip`; `pkg/balance/router/router.go:Keyspace`; `pkg/balance/observer/backend_fetcher.go:GetBackendList` | `pkg/balance/router/group_test.go:TestCrossKeyspaceFailoverRefusesRedirect`, `TestEmptyToNonemptyKeyspaceFailoverRefused`, `TestSameKeyspaceFailoverStillRedirects`, `TestEmptyKeyspaceFailoverStillRedirects`, `TestCrossKeyspaceRefusalIsBounded`; `pkg/balance/router/router_test.go:TestBackendKeyspacePrefersPathParsedOverLabel`; `tests/dataplane/integration/run.sh` no-keyspace-migration phase (both modes) | `dataplane`, `control-proto` | `PARITY-MIG-008` | GO-VERIFIED / RUST-VERIFIED (the guard sits at the shared Go issuance boundary, so the Rust dataplane's projected sessions receive only keyspace-stable RedirectCommands - proven E2E with a pinned session under real migration pressure in rust mode) | P0 | P0 |
| MIG-009 | Opt-in observation captures actual reservation, creation, migration, close and rehydration under existing Go locks. The native factory additionally captures the ordered query, clock and getter values used by all three native factor entrypoints, then publishes the complete evaluation before the same Group unlock. Rust independently computes factors and compares bounded output witnesses without routing or accounting authority; failed comparisons preserve the prior prefix and invalidate qualification. | `pkg/balance/router/observation.go`; `pkg/balance/router/group.go:publishPolicyObservationLocked`; `pkg/balance/factor/native_capture.go`; `pkg/balance/observation/`; `pkg/controlbridge/shadow/` | `pkg/balance/router/observation_test.go`; `pkg/balance/router/native_observation_test.go`; `pkg/balance/router/native_load_test.go`; `rust/crates/control-router/src/shadow/live/native_tests.rs`; `rust/crates/legacy-router-shadow/src/native_tests.rs`; `tests/controlplane/cproute/shadow/live-run.sh`; `tests/controlplane/cproute/shadow/native-run.sh` | `control-router`, `legacy-router-shadow` | `PARITY-MIG-009` | GO-IMPLEMENTED / RUST-IMPLEMENTED (222-3 slice 2B1: v1/v2 lifecycle and v3 native factor comparison; actual selector/reservation choice, scheduler decisions, full SQL acceptance and Rust routing activation remain uncovered; selection=false and scheduler=false) | P1 | P1 |

## Admission, routing, configuration, and topology

| ID | Required behavior and invariant | Go source | Go test evidence | Rust owner | Target test ID | Status | Canary | Cutover |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| ADM-001 | Reject a new TCP connection before allocating session state when memory usage exceeds the configured threshold; increment `reject_conn_total{reason="memory"}`. | `pkg/proxy/proxy.go:rejectConnByMemory`; `lib/config/proxy.go:HighMemoryUsageRejectThreshold` | `pkg/proxy/proxy_test.go:TestRejectConnByMemory`; `lib/config/proxy_test.go:TestProxyCheck`; `rust/crates/dataplane/src/server.rs:memory_rejection_precedes_id_and_reload_applies_to_new_connection`; `rust/crates/dataplane/src/observability.rs:native_exposition_matches_go_golden` | `dataplane`, `control-proto` | `PARITY-ADM-001` | GO-VERIFIED / RUST-VERIFIED (the rejection happens **before any session state exists**: `server.rs:867` runs `admission.try_acquire` ahead of `configure_stream` and `registry.register`, and the test proves the ordering by consequence rather than by inspection -- after a memory reject the client socket reads EOF, `rejected_memory_total` is 1, `registered_total` is 0, the registry is empty, and the *next* admitted connection still takes connection id **1**, so no id was burned. Raising the probe and reloading a zero threshold then admits it, which also pins the live-reload half. Exposition: `observability.rs:1815` emits `tiproxy_server_reject_connection_total{type="memory"}`, one of the two label values `pkg/metrics/rust_metrics.go:validateRustLabelValues` accepts for this family) | P1 | P0 |
| ADM-002 | Enforce live `max-connections` (`0` means unlimited) before creating a connection ID; reject and increment the max-connections reason metric. | `pkg/proxy/proxy.go:onConn`; `lib/config/proxy.go:MaxConnections` | `pkg/proxy/proxy_test.go:TestCreateConn`, `TestWatchCfg`; `rust/crates/dataplane/src/server.rs:bounded_connections_reject_at_boundary_without_gauge_drift`; `rust/crates/dataplane/src/observability.rs:native_exposition_matches_go_golden` | `dataplane`, `control-proto` | `PARITY-ADM-002` | GO-VERIFIED / RUST-VERIFIED (all three clauses are pinned in one test. **At the boundary**: with `max-connections = 2` the third connection reads EOF, the registry holds exactly 2, `active_connections` is 2 and `rejected_max_connections_total` is 1. **`0` means unlimited**: reloading `max-connections = 0` then admits three more past the old bound. **Before creating a connection ID**: those three take ids 3, 4, 5 -- the reject burned nothing. Both halves have negative controls: dropping the `max_connections != 0` guard in `admission.rs:317` makes every connection under the unlimited policy reject (`active >= 0` always holds) and the test times out; allocating an id in the rejection arm of `server.rs:869` makes the next admitted connection take 4 instead of 3 and fails BOTH this row's test and ADM-001's. Exposition: `tiproxy_server_reject_connection_total{type="max_connections"}` carries a recorded delta in `tests/dataplane/metrics/parity-batches.json` and renders byte-identical to the Go `promhttp` oracle) | P1 | P0 |
| ADM-003 | Select a backend with listener port, real client address, and proxy address; retry distinct candidates with bounded dial/connect timeouts and unregister abandoned routes. CTL-06 adds the `AssignmentRehydrator` seam (`RehydrateConn`/`LookupBackend` on `Group`, `ScoreBasedRouter`, `StaticRouter`): reconciliation after a Go restart re-attaches an established connection to its backend exactly as a successful assignment (score, connection list, event receiver, returned `BackendInst`) without running selection, so later redirect/CLOSED accounting retires exactly once.  Test-build API recording uses whole observer/config/timer inputs and records selector-scope termination, accepted-operation callbacks and sealed archives; abandoned selectors must not disappear from the final checkpoint. | `pkg/proxy/backend/backend_conn_mgr.go:getBackendIO`; `pkg/balance/router/` (`AssignmentRehydrator`)  `pkg/balance/router/replay_driver_apireplay.go`; `tests/controlplane/cproute/api-differential/recorder/` | `pkg/proxy/backend/backend_conn_mgr_test.go:TestGetBackendIO`, `TestGetBackendIOUsesBackendDialContext`, `TestConnectFail`; (the Go `reconcile_rehydrate_test.go` rehydration tests were deleted at #223 Phase 2 with `router_adapter.go`) `rust/crates/dataplane/tests/control_commands.rs:reconcile_request_carries_rehydration_fidelity`, `reconcile_reports_current_incarnation_same_generation_new_identity`, `rust_restart_clears_ghosts_and_replays_lost_results`, `redirect_replay_survives_epochs_and_generations_stay_separate`; `tests/dataplane/integration/run.sh` chain-c go-one-sided-restart phase ; `recorder/apireplay/apireplay_test.go:TestAbandonedSelectorEndsWithoutInventedFinish`, `TestReviewRedirectMustBindAcceptedOperationAndDestination`; `recorder/harness/scheduler_test.go:TestArchiveIsExclusiveAndSealed`; `recorder/cmd/record/main_test.go:TestLedgerTracksAbandonedAndClosedSessions` | `control-proto`, `dataplane` | `PARITY-ADM-003` | GO-VERIFIED / RUST-VERIFIED (**the following describes the legacy gate path, retired at #223 Phase 2 — see the scope note at the end of this cell for what runs now.** Under it the Rust dataplane was the exactly-once reconcile *reporter*, not the re-attacher: `build_reconcile_request` carried the exact `connection_id`-consistent identity, authoritative backend (not a pending redirect target), admitted snapshot generation (not the method's known-generation), pending-redirect id, and command watermark that Go's `RehydrateConn` consumes to re-attach without running selection; a same-generation/new-epoch incarnation reports the current identity with no drift and is build-stable. **The Go re-attachment path is no longer live.** #223 Phase 2 deleted the bridge adapter that was `RehydrateConn`'s only production caller, and the residual route-owner handler refuses a reconcile carrying connections rather than re-attaching from it; `AssignmentRehydrator` survives in `pkg/balance/router` as a test-exercised reference implementation with no production caller. The Go-side retire-exactly-once was proven by the Go same-gen/new-epoch tests, which were deleted with the adapter, and by the `chain-c` real-TiDB Go-restart E2E — that qualification is **historical Go-adapter evidence**, not a current mechanism. **The full reconcile reporter and ghost-repair mechanism described above is likewise old-protocol history.** Under the route owner, `control_dispatch.rs:1802` takes the `build_residual_reconcile_request` path, which sends `connections: Vec::new()` — an empty residual ACK carrying only generation and watermarks. No connection identity crosses the bridge, so nothing reports ghosts and nothing repairs them; what the residual path establishes is only that Go does **not** restore or re-attach SQL sessions. **It says nothing about what survives a Rust owner restart**: no test here claims the original SQL connections are preserved across one, and this row must not be read as promising that. Current evidence: `route_owner_reconcile_rejects_route_state_and_accepts_residual_ack`)  API-recorder lifecycle: GO-IMPLEMENTED / RUST-CORPUS-PENDING (same-tree recorded replay, engine-relative effects and metrics/cadence remain acceptance requirements; existing reconcile evidence does not qualify the new corpus). | P0 | P0 |
| ADM-004 | Preserve public-endpoint classification from the PROXY address/CIDR or public IP and pass cluster name to the backend dialer. | `pkg/proxy/proxy.go:fromPublicEndpoint`, `onConn` | `pkg/proxy/proxy_test.go:TestPublicEndpoint`; `pkg/proxy/backend/backend_conn_mgr_test.go:TestGetBackendIOUsesBackendDialContext`; `rust/crates/dataplane/src/metering.rs:an_address_outside_every_cidr_is_public_unless_go_calls_it_private`, `public_endpoint_uses_direct_peer_cidrs`; `rust/crates/dataplane/tests/route_control.rs:cluster_tcp_dialer_is_cluster_aware_and_observable`, `tcp_dialer_connects_and_stays_cluster_unaware` | `dataplane`, `control-proto` | `PARITY-ADM-004` | GO-VERIFIED / RUST-VERIFIED (**this row was a real divergence, not a stale label.** `is_public_endpoint` matched the configured public CIDRs and stopped there, but Go `fromPublicEndpoint` returns `true` on a CIDR hit **or** when `netutil.IsPrivate(ip)` is false. Under the default configuration -- no `public_cidrs` at all -- Go classifies every genuinely public client as public and Rust classified none, so the public share of metered bytes was always zero. The fallback now mirrors Go's private set exactly, which is wider than RFC1918: loopback, link-local unicast *and* link-local multicast, the 100.64.0.0/10 carrier-grade NAT range, and `net.IP.IsPrivate`. Go reaches all of them through `To4()`, so a v4-mapped v6 address is judged by its v4 form and the Rust side normalizes once up front to reproduce that. Six negative controls, each failing on its own address: neutralizing the fallback fails `8.8.8.8`, and neutralizing the CGNAT, link-local-multicast, v6 link-local, v4 link-local, or v4-mapped-normalization branch fails `100.64.0.1`, `224.0.0.251`, `ff02::1`, `169.254.1.1` and `::ffff:10.0.0.1` respectively. The pre-existing test kept passing throughout because its only negative cases were private addresses -- which is exactly how the gap stayed hidden. **The classified address is the direct accepted peer, not the PROXY-v2 inner client**, and that is correct rather than an omission: Go's `ProxyAddr()` returns the underlying `packetReadWriter.RemoteAddr()` even when a PROXY header supplied a different client address, so both sides classify the hop visible to the listener. Cluster identity on the dial: the assignment carries `cluster_name`, the cluster-aware dialer records it, and a cluster-scoped assignment reaching the cluster-unaware direct dialer fails closed with `ClusterUnsupported` rather than dialing outside the scope) | P1 | P1 |
| CFG-001 | Atomically apply reloadable dataplane fields to new sessions: admission limits, buffer size, frontend/backend keepalive, PROXY v2, backend TLS requirement, shutdown timers, public endpoints, and backend cluster routing. Existing sessions retain connection-scoped protocol settings except health-driven backend keepalive. The Rust dataplane is explicitly gated; Go publishes monotonic generations and resends the desired snapshot after reconnect; Rust atomically swaps one complete last-good generation for new admissions, rejects listener changes after the first bind, and preserves the prior generation on rejection. | `pkg/proxy/proxy.go:reset`; `lib/config/proxy.go`; `pkg/controlbridge/snapshot.go`, `snapshot_publisher.go`; `pkg/server/rust_dataplane.go`; Rust last-good store in `rust/crates/control-proto/src/snapshot.rs`; Rust generation owner in `rust/crates/dataplane/src/runtime_config.rs`; `rust/crates/tiproxy-rs/src/main.rs` | `pkg/proxy/proxy_test.go:TestWatchCfg`; `lib/config/proxy_test.go:TestProxyCheck`, `TestCloneConfig`, `TestRustDataplaneConfigGateAndClone`; `pkg/controlbridge/snapshot_test.go`, `snapshot_publisher_test.go`; `pkg/server/server_test.go:TestRustDataplaneGateOwnsNoGoListenerAndCloses`; `rust/crates/control-proto/tests/snapshot_store.rs`; `rust/crates/dataplane/src/server.rs:existing_session_keeps_admission_snapshot_and_observes_live_generation`; `rust/crates/dataplane/tests/runtime_config.rs:first_bind_reload_reject_and_shutdown_keep_one_last_good_generation`; `rust/crates/dataplane/tests/session_engine.rs:existing_session_reapplies_keepalive_on_backend_health_flip` | `control-proto`, `dataplane` | `PARITY-CFG-001` | GO-VERIFIED / RUST-PARTIAL (complete-generation validation, last-good publication, admission reload, immutable established-session config, and the explicit live backend-health selector are verified. **The row's remaining scope used to read "field-specific reload acceptance" with no field list, which gave it no completion standard. It is now enumerated.** The classification is settled by the architecture rather than per field: `session_engine.rs` takes connection-scoped config from `seat.snapshot()`, frozen at admission, so a reload reaches the *next* admission only, and existing sessions consult live state for backend health alone. **Verified per field**: `max_connections` and `high_memory_reject_threshold` (ADM-002/ADM-001 reload tests), healthy/unhealthy backend keepalive as the one established-session exception (`existing_session_reapplies_keepalive_on_backend_health_flip`), `listeners` as restart-only (`first_bind_reload_reject_and_shutdown_keep_one_last_good_generation`), backend-cluster routing (TOPO-003), and now `connection_buffer_bytes`, `frontend_keepalive` and `public_cidrs` -- `rust/crates/dataplane/src/server.rs:reloaded_buffer_size_applies_to_the_next_admission_only`, `reloaded_frontend_keepalive_reaches_the_next_socket`, `reloaded_public_cidrs_change_only_the_next_connections_class`. Each observes the *effect* rather than the new snapshot's contents -- the reservation gauge, a socket keepalive readback, and the same `metering::is_public_endpoint` call the session path makes -- and one negative control covers all three: validating a reload without publishing it fails each on its own message. **Remaining: two fields plus one deferred item.** `proxy_protocol` and `require_backend_tls` both read from `seat.snapshot()` the same way, but their acceptance needs an adaptive fake backend -- the shared one decides at spawn whether to expect a PROXY preamble and `strip_inbound_proxy_v2` consumes the magic bytes before it can tell, so a mid-run flip corrupts the stream in whichever direction it is driven. `graceful_close_millis` is deliberately **not** given its own fixture here: it shares the process-shutdown coordinator seam with LIFE-001 and should be proven by one coordinator test once orderly and fault shutdown are split, rather than by scaffolding that the two-clock change would rewrite) | P1 | P0 |
| CFG-002 | Reject unsupported PROXY versions and invalid buffer sizes, listener ranges, backend-cluster definitions, keepalive values, thresholds, and failover values before they reach Rust. | `lib/config/proxy.go:Check`, `ProxyServer.Check`; `pkg/controlbridge/snapshot.go`; Rust bounds validation in `rust/crates/control-proto/src/snapshot.rs` | `lib/config/proxy_test.go:TestProxyCheck`; `pkg/controlbridge/snapshot_test.go`; `rust/crates/control-proto/tests/snapshot_store.rs:config_bounds_are_rejected_without_disturbing_last_good`, `expiry_ca_tls_policy_and_unsupported_configuration_are_rejected_atomically`, `tls_paths_must_remain_beneath_the_allowlist`; `rust/crates/control-config/tests/source.rs:topology_projection_normalizes_clusters_and_tls`, `ns_servers_are_normalized_sorted_stably_with_duplicates_preserved` | `control-proto` | `PARITY-CFG-002` | GO-VERIFIED / RUST-VERIFIED (Go's `ProxyServer.Check` is the primary gate; `control-proto::snapshot::validate_config` is the last line before a bad generation becomes the one every new session reads, and **its rejection arms had no test at all** until now. Eighteen one-field-at-a-time mutations of an otherwise-valid snapshot each produce their own typed `Invalid` naming their own field, and none of them disturbs the retained last-good generation -- buffer size below 1 KiB and above 16 MiB, memory threshold above 1.0, NaN, and inside Go's forbidden `0 < t < 0.5` band, an unspecified or out-of-enum PROXY mode, zero listeners, a zero or beyond-`u16` listener port, an unnamed listener, duplicate listener name and duplicate address, each of the three keepalive policies absent, an empty server version, and a malformed public CIDR. Two negative controls: neutralizing the forbidden-band clause or the duplicate-address clause each fails exactly its own case and no other. **Scope on the row's remaining nouns**: `proxy.failover-timeout` is not a `ConfigSnapshot` field at all, so it can only be rejected in Go and Rust has nothing to validate; backend-cluster definitions reach Rust through `control-config`, whose `normalized_backend_clusters` rejects an empty `pd-addrs` and mirrors Go `normalizeCluster`'s sort-raw-then-normalize order; and neither side bounds keepalive *values* -- Go's `KeepAlive` has no `Check` arm for idle/cnt/intvl/timeout, so requiring the policy to be present is strictly stricter than Go, not weaker) | P1 | P1 |
| CFG-003 | Routing lifecycle observation defaults off; the explicit routing-shadow-socket is restart-required and must be absolute, distinct from the control socket, and used with the Rust dataplane. Startup rollback and shutdown join the optional consumer; transport failures stay diagnostic. | `lib/config/proxy.go:RustDataplane`, `Check`; `pkg/server/server.go:NewServer`; `pkg/manager/namespace/manager.go:buildNamespace` | `lib/config/proxy_test.go:TestRoutingObservationConfigIsExplicitAndRestartPinned`; `rust/crates/control-config/tests/source.rs:routing_observer_socket_is_disabled_by_default_validated_and_restart_pinned`; `rust/crates/tiproxy-rs/src/main.rs` observer CLI, rollback and commit tests | `control-config`, `tiproxy-rs`, `legacy-router-shadow` | `PARITY-CFG-003` | GO-IMPLEMENTED / RUST-IMPLEMENTED (optional lifecycle diagnostics only; no production routing authority) | P1 | P1 |
| CFG-004 | Keep `rust-dataplane.metrics-owner-port` restart-required in both config models. Zero selects an ephemeral metrics-owner port; an explicit port must fit `uint16` and differ from every SQL listener (including port ranges) and the API port. Reject invalid candidates before publication; a Rust reload cannot change this port or replace the retained generation. | `lib/config/proxy.go:RustDataplane`, `checkRustMetricsOwnerPort`; `rust/crates/control-config/src/model.rs:validate_metrics_owner_port`; `rust/crates/tiproxy-rs/src/topology_composition.rs:metric_owner_bind_address` | `lib/config/proxy_test.go:TestMetricsOwnerPortIsExplicitRestartPinnedAndCollisionFree`; `rust/crates/control-config/tests/source.rs:metrics_owner_port_is_explicit_restart_pinned_and_collision_free` | `control-config`, `tiproxy-rs` | `PARITY-CFG-004` | GO-IMPLEMENTED / RUST-IMPLEMENTED (existing config tests cover the zero default, explicit port, SQL/API collisions, out-of-range decode and restart-pinned Rust reload; this row records the C10 contract without changing product code) | P1 | P0 |
| TOPO-001 | Bind every configured SQL address or inclusive port range, report actual listener addresses (including ephemeral ports), accept independently, and stop all listeners together. | `pkg/proxy/proxy.go:NewSQLServer`, `Run`, `PreClose`; `lib/config/proxy.go:GetSQLAddrs`; snapshot projection in `pkg/controlbridge/snapshot.go` | `pkg/proxy/proxy_test.go:TestMultiAddr`, `TestPortRange`, `TestConnAddrUsesActualListenerAddr`; `lib/config/proxy_test.go:TestGetSQLAddrs`; `pkg/controlbridge/snapshot_test.go:TestSnapshotBuilderBuildsCompleteDeterministicSnapshot`; `rust/crates/dataplane/src/server.rs:expanded_port_range_binds_all_and_releases_together` (three contiguous ports bound from one snapshot, each accepted on its own owner under its own name/address while the first connection's handler is parked, all released together), `actual_address_keepalive_and_shutdown_cleanup` (a port-0 listener reports its real ephemeral port and hands that same address to the session as `listener_address`) | `dataplane`, `control-proto` | `PARITY-TOPO-001` | GO-VERIFIED / RUST-VERIFIED (**scope**: the `host:start-end` *textual* expansion is not Rust's -- `lib/config/proxy.go:GetSQLAddrs` expands it and `pkg/controlbridge/snapshot.go:buildListeners` projects one `Listener` per port, so the Rust dataplane receives an already-expanded list and CFG-002 owns rejecting a bad range. What is verified natively is the dataplane's own contract: `server.rs:bind_specs` binds every listener in the snapshot or drops all sockets from the attempt, `run` spawns one `run_listener` owner per listener, and shutdown closes every one. Negative control for the independent-accept clause: keeping the later listeners bound but never spawning their owners makes the test time out at `listener did not accept` while the sockets still answer `connect`, so it discriminates on accepting rather than on binding) | P0 | P0 |
| TOPO-002 | Route a handshake by configured user namespace, fall back to `default`, and expose namespace identity in logs; namespace additions/removals must not cross-route established sessions. | `pkg/proxy/backend/handshake_handler.go:GetRouter`; `pkg/proxy/backend/static_handshake_handler.go`; `pkg/manager/namespace/`; `pkg/balance/router/topology.go`; `pkg/server/rust_dataplane.go:projectControlTopology` | `pkg/manager/namespace/manager_test.go`; `pkg/proxy/backend/backend_conn_mgr_test.go:TestCustomHandshake`; `pkg/server/rust_dataplane_test.go:TestProjectControlTopology`; `pkg/controlbridge/snapshot_publisher_test.go:TestSnapshotPublisherCarriesTopologyProjection` | `control-proto`, `dataplane` | `PARITY-TOPO-002` | GO-IMPLEMENTED / RUST-PARTIAL (DPL-07 layer 1: the wire snapshot carries the live namespace/backend topology via the score router's deterministic enumeration and `projectControlTopology`, and the real-TiDB topology proves the matrix in BOTH modes: admin-API-seeded namespaces with pinned static backends, `SELECT @@port` per user — `tests/dataplane/integration/run.sh --mode {go,rust} --variant plain`. **The control-seam assertions this row used to cite — same handler and same projected connection across three username×cluster combinations, listener-port classes, the two-cluster port conflict, unknown-namespace/no-backend error text and dynamic-change keyspace stability — were Go-adapter qualification and went with `namespace_parity_test.go` at #223 Phase 2.** They are history, not current evidence. What still holds natively: listener-port classes and the port conflict via `pkg/balance/router`'s group/port observation, and namespace resolution via `control-router::namespace::tests`; the client-visible error text has no Rust assertion. **That is this row's only residual, and it is an accepted test gap rather than pending work**: bb7133 accepted it explicitly when `namespace_parity_test.go` was deleted at #223 Phase 2, on the grounds that internal coverage reaches it, and it is tracked as task #111. The status stays `RUST-PARTIAL` because that is exactly what the legend describes -- a verified bounded subset with the remaining gap stated -- not because anyone is still working on it. Lifting the row to VERIFIED would claim an assertion that does not exist; dropping to IMPLEMENTED would discard the real-TiDB two-mode topology matrix and `control-router::namespace::tests` cited above) | P0 | P0 |
| TOPO-003 | Support multiple named backend clusters, carry cluster identity on each selected backend/dial, isolate health/routing state per cluster, and retain legacy single `pd-addrs` fallback. | `lib/config/proxy.go:GetBackendClusters`; `pkg/manager/backendcluster/`; `pkg/proxy/proxy.go:onConn` | `lib/config/proxy_test.go:TestGetBackendClusters`; `pkg/manager/backendcluster/*_test.go`; `pkg/server/api/config_test.go`; `rust/crates/control-config/tests/source.rs:topology_projection_normalizes_clusters_and_tls`, `legacy_pd_addrs_fall_back_to_default_cluster`; `rust/crates/control-router/src/tests.rs:equal_listener_ports_in_different_clusters_block_reservation`; `rust/crates/control-topology/src/backend_health.rs:a_sibling_cluster_backend_is_rejected_before_any_io`; `rust/crates/dataplane/tests/route_control.rs:tcp_dialer_connects_and_stays_cluster_unaware`, `cluster_tcp_dialer_is_cluster_aware_and_observable` | `control-proto`, `dataplane` | `PARITY-TOPO-003` | GO-IMPLEMENTED / RUST-VERIFIED (all four clauses hold natively. Multiple named clusters and the legacy single-`pd-addrs` fallback: `control-config::model::normalized_backend_clusters` mirrors Go `normalizeCluster` (including its sort-raw-then-normalize order for `ns-servers`) and synthesizes the `default` cluster from `pd-addrs` only when no `[[proxy.backend-clusters]]` is declared. Cluster identity on each dial: the assignment carries `cluster_name` and a dialer declares `CLUSTER_AWARE`, so a cluster-scoped assignment reaching the direct dialer fails closed with `AcquireError::ClusterUnsupported` instead of dialing outside the scope. Per-cluster isolation of routing and health state: two clusters holding the SAME backend address on the SAME listener port keep separate ledger accounting under `default/127.0.0.1:4000` and `second/127.0.0.1:4000` and the reservation is refused with `RouteError::PortConflict`, and a cluster-stamped `ClusterHealthNetwork` refuses a sibling cluster's backend *before any dial* (`accepted == 0`), so one cluster's DNS/TLS material can never probe another's. **Scope**: the dataplane's own `route_control.rs:ClusterTcpDialer` resolves with the system resolver because `BackendCluster.NSServers` is not a field of the wire snapshot -- per-cluster explicit nameservers live in the route owner's `control-external::ClusterConnector::from_cluster_material`, built from cluster config material rather than from the snapshot. Extending the snapshot for a serverless projection is DPL-07 follow-up, not a hole in the four clauses above) | P0 | P0 |

## Keepalive, accounting, and observability

| ID | Required behavior and invariant | Go source | Go test evidence | Rust owner | Target test ID | Status | Canary | Cutover |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| KA-001 | Apply frontend TCP keepalive enablement and platform-specific idle/count/interval/timeout semantics; disabled keepalive still applies the configured user timeout where supported. | `pkg/proxy/keepalive/`; `pkg/proxy/proxy.go:onConn` | `pkg/proxy/net/packetio_test.go:TestKeepAlive`; `pkg/proxy/backend/backend_conn_mgr_test.go:TestKeepAlive`; `rust/crates/proxy-io/tests/socket_policy.rs:keepalive_exact_values_on_linux`, `keepalive_switch_is_observable_portable`; `rust/crates/dataplane/src/server.rs:actual_address_keepalive_and_shutdown_cleanup`; `rust/crates/dataplane/tests/session_engine.rs:existing_session_reapplies_keepalive_on_backend_health_flip` | `proxy-io`, `dataplane` | `PARITY-KA-001` | GO-VERIFIED / RUST-VERIFIED (the values are read back off a live accepted socket, not merely written: on Linux the exact Go `DefaultKeepAlive` quadruple is observable for both policies -- healthy 60 s idle / 5 probes / 3 s interval / 15 s `TCP_USER_TIMEOUT`, unhealthy 10 s / 5 / 1 s / 5 s. **The disabled clause is the load-bearing one and it holds**: turning `enabled` off still leaves `user_timeout` at 5 s, exactly like Go's `setTimeout` living outside the enabled branch. Platform handling is explicit rather than silent: off-Linux a nonzero user timeout returns `SocketError::UnsupportedPlatform` instead of quietly succeeding, and the portable test asserts that diagnostic directly while still proving enablement and the healthy/unhealthy idle switch everywhere on Unix. **CI dependency**: `keepalive_exact_values_on_linux` is `#[cfg(target_os = "linux")]`, so the exact quadruple is asserted only on the Linux runner -- a macOS-only run proves enablement and the switch, not the values. On the production path `server.rs` applies `policy.frontend_keepalive` to each accepted stream after registration and continues serving on failure, matching Go, and an established session re-applies backend keepalive on a health flip) | P1 | P1 |
| KA-002 | Apply conservative keepalive to healthy backends and aggressive keepalive to unhealthy backends; re-evaluate on health ticker and immediately after migration. | `pkg/proxy/backend/backend_conn_mgr.go:setKeepAlive`; `lib/config/proxy.go:DefaultKeepAlive` | `pkg/proxy/backend/backend_conn_mgr_test.go:TestKeepAlive`, `TestBackendStatusChange`; `rust/crates/dataplane/src/server.rs:existing_session_keeps_admission_snapshot_and_observes_live_generation`; `rust/crates/dataplane/src/session_engine.rs:live_backend_health_requires_the_exact_backend_identity`; `rust/crates/dataplane/tests/session_engine.rs:existing_session_reapplies_keepalive_on_backend_health_flip` | `dataplane`, `control-proto` | `PARITY-KA-002` | GO-VERIFIED / RUST-VERIFIED (the single socket owner consumes complete last-good generations, matches exact backend id/address/cluster, treats absence or mismatch as unhealthy, re-selects the immutable admission policy at command-safe boundaries and immediately after attach/swap, retries best-effort failures every five seconds, and exports a bounded success/failure counter) | P1 | P0 |
| KA-003 | Probe an idle backend at `CheckBackendInterval`; if peer EOF is observed, classify backend-network exit and close the client. Do not probe concurrently with command I/O. | `pkg/proxy/net/packetio.go:IsPeerActive`; `pkg/proxy/backend/backend_conn_mgr.go:checkBackendActive` | `pkg/proxy/net/packetio_test.go:TestPeerActive`; `pkg/proxy/backend/backend_conn_mgr_test.go:TestBackendInactive`; `rust/crates/dataplane/tests/session_engine.rs:idle_probe_consumed_byte_is_counted_in_raw_backend_in`, `existing_session_reapplies_keepalive_on_backend_health_flip`; `rust/crates/dataplane/tests/session_loop.rs` | `proxy-io`, `dataplane` | `PARITY-KA-003` | GO-VERIFIED / RUST-VERIFIED (idle-safe probing and health-driven keepalive both run on the single socket owner, never concurrently with command I/O; peer EOF is classified as backend-network exit and live health changes preserve command framing) | P1 | P0 |
| MTR-001 | After handshake and every completed/held command, account backend inbound/outbound bytes and physical packets by backend; count cross-location traffic in both directions. | `pkg/proxy/backend/backend_conn_mgr.go:updateTraffic`; `pkg/proxy/backend/metrics.go:addTraffic`; `pkg/metrics/traffic.go` | `pkg/proxy/backend/backend_conn_mgr_test.go:TestTrafficMetrics`; `rust/crates/dataplane/tests/session_engine.rs:session_path_emits_query_traffic_and_exact_quit_source`, `outbound_proxy_v2_header_counts_once_in_raw_backend_out`; `rust/crates/proxy-io/tests/counted.rs`; `rust/crates/proxy-io/tests/tls_matrix.rs:raw_counter_below_tls_counts_record_bytes_not_plaintext`; `dataplane::observability::histogram_wire_semantics_are_exact_and_cumulative`; `pkg/metrics:TestRustMetricsHistogramMergesExactBuckets` | `dataplane` | `PARITY-MTR-001` | GO-VERIFIED / RUST-IMPLEMENTED (WIRE-MTR: byte totals come from the innermost `CountedIo` raw-socket seam — Go `basicReadWriter` parity, so counts track real TLS-record/PROXY/compressed wire bytes — while physical-packet counts stay at the framing layer; per-socket counters mean migration close-out lands with MIG-007) | P1 | P1 |
| MTR-002 | Meter per-keyspace response bytes, cross-AZ bytes, and public/private origin. The keyspace is connection-stable and deltas must remain correct across migration. | `pkg/proxy/backend/backend_conn_mgr.go:updateTraffic`; `pkg/proxy/backend/backend_conn_mgr.go:Meter`; `pkg/manager/meter/` | `pkg/proxy/backend/backend_conn_mgr_test.go:TestTrafficMetrics`; `pkg/manager/meter/meter_test.go:TestWrite`, `TestLoop`; `rust/crates/dataplane/src/metering.rs:native_sampler_delivers_final_batch_without_control_bridge`, `public_endpoint_uses_direct_peer_cidrs`, `unknown_keyspace_is_session_scoped_not_global_failure`; `rust/crates/dataplane/tests/session_engine.rs:metering_redirect_unknown_keeps_old_and_success_starts_new_generation`, `metering_unknown_initial_keyspace_rejects_before_billable_forwarding` | `dataplane`, `control-proto` | `PARITY-MTR-002` | GO-VERIFIED / RUST-VERIFIED (the three metered quantities match Go `updateTraffic` arithmetic exactly: the response-byte figure is the *inbound* delta (10), `cross_az_bytes` is inbound+outbound (30) and is charged only when the backend is not `local`, and the public/private split routes the same bytes to `public_response_bytes` by the direct accepted peer's CIDR class rather than by any forwarded header. The keyspace is a per-attribution label and an empty one is never billable -- Go skips `IncTraffic` under `len(keyspace) > 0`, Rust refuses to register the source at all and the refusal is session-scoped, not a global meter failure. **Across migration** the deltas do not merge: redirecting a session from `keyspace-a` to `keyspace-b` leaves the old generation's final source and the new generation's source coexisting until the WAL handoff, so neither generation's bytes land under the other's keyspace) | P1 | P0 |
| MTR-003 | Preserve command-count and duration labels, handshake/backend-selection duration, connection age/lifetime, connection gauge/create/disconnect/reject counters, backend keepalive update outcomes, and error-source labels with their existing Prometheus names. | `pkg/proxy/backend/metrics.go`; `pkg/metrics/session.go`, `server.go`, `backend.go`, `metrics.go`, `rust_metrics.go`; `pkg/proxy/proxy.go`; `pkg/proxy/client/client_conn.go` | `pkg/proxy/backend/backend_conn_mgr_test.go:TestTrafficMetrics`; `rust/crates/dataplane/tests/session_engine.rs:session_path_emits_query_traffic_and_exact_quit_source`, `existing_session_reapplies_keepalive_on_backend_health_flip`; `pkg/metrics:TestRustMetricsCounterSequenceDedup`, `TestRustMetricsGaugeReconcilesAcrossEpoch`, `TestRustMetricsHistogramMergesExactBuckets`, `TestRustMetricsBackendKeepAliveUpdateMerges`; `dataplane::observability::queue_full_is_non_blocking_and_counted`, `backend_keepalive_update_metric_uses_bounded_labels`, `native_exposition_matches_go_golden`, `registry_is_the_cumulative_twin_of_the_bridge_deltas`; `tests/dataplane/metrics/gen` (Go `promhttp` oracle over `tests/dataplane/metrics/native-families.json`, gated by `make dataplane-metrics-parity`); `tiproxy-rs::metrics_http::routes_metrics_paths_and_rejects_others` | `dataplane`, `control-proto` | `PARITY-MTR-003` | GO-IMPLEMENTED / RUST-IMPLEMENTED (the Rust observation stream exports a bounded `{backend,health,result}` backend-keepalive counter and the Go bridge accepts only the closed label vocabulary before merging it into Prometheus; the same closed catalog is also served natively by the Rust process on `--metrics-addr` (`/metrics`, `/api/metrics`) from a cumulative registry fed by the identical aggregator mapping, and the native text exposition is pinned byte-for-byte to the Go `promhttp` output for the recorded delta batches) | P1 | P1 |
| MTR-004 | Preserve migration success/failure/pending metrics and backend connection gauges as Go control-plane observations of Rust lifecycle events. | `pkg/metrics/balance.go`; `pkg/balance/router/` (the `pkg/controlbridge/router_adapter.go` producer was deleted at #223 Phase 2) | `pkg/balance/router/router_score_test.go:TestRebalance`, `TestRedirectFail`, `TestCloseRaceWithRedirectSucceed`, `TestCloseRaceWithRedirectFail`; `rust/crates/dataplane/tests/session_engine.rs:redirect_restores_candidate_and_swaps_atomically`, `candidate_failures_preserve_old_backend` | `control-proto`, `dataplane` | `PARITY-MTR-004` | GO-IMPLEMENTED / RUST-IMPLEMENTED — **mechanism superseded, see MTR-006.** This row's premise was that these families are *Go control-plane observations of Rust lifecycle events*. That is no longer how they are produced: #223 Phase 2 retired `ConnectionEvent` to a tombstone, so no Rust terminal feeds a Go router, and the Go observation path is therefore gone. **Replaced by CP-ADMIN slice 5d**, which serves the migration and backend-connection families natively from Rust's own routing state (MTR-006, now 12 of 12), with MTR-007 and MTR-009 recording where those values deliberately differ from Go's. What the cited Go tests still establish is the **Go-side baseline** the native implementation was matched against — the pending gauge returns to baseline exactly once and a duplicate cannot decrement twice — not a live Go observation path | P1 | P0 |
| MTR-005 | Attach connection ID, listener address, real/proxy client address, namespace, backend address, capabilities, and quit source to the same lifecycle/error logs without auth payloads or secrets. | `pkg/proxy/proxy.go:onConn`; `pkg/proxy/backend/backend_conn_mgr.go:ConnInfo`; `pkg/proxy/backend/authenticator.go:ConnInfo` | `pkg/proxy/backend/backend_conn_mgr_test.go:TestConnID`, `TestConnAttrs`, `TestDisconnectLog`; `dataplane::observability::log_schema_is_closed_payload_free_and_bounded`; `dataplane::session_engine::proxy_client_log_address_prefers_inbound_inet_source_and_falls_back_to_peer`, `proxy_client_log_source_is_shared_with_the_force_close_owner`; `rust/crates/dataplane/tests/session_engine.rs:session_path_emits_query_traffic_and_exact_quit_source`, `inbound_proxy_v2_header_and_tlvs_are_forwarded_verbatim`; `tests/dataplane/integration/run.sh:MTR-005 lifecycle-log attribution` | `dataplane`, `control-proto` | `PARITY-MTR-005` | GO-VERIFIED / RUST-VERIFIED (lifecycle logs keep `client_addr` as the accepted TCP peer and use the parsed inbound PROXY inet source for `proxy_client_addr`, falling back to the peer for direct, LOCAL, UNSPEC, and Unix-address sessions; the immutable source is shared with the close owner so force-abort cannot regress attribution, while public/private metering remains keyed by Go-compatible TCP-peer `ProxyAddr()` semantics) | P2 | P1 |
| MTR-006 | Reimplement natively, from Rust's own state, the metric families that only Go produced and that left the exposition when Rust took over `/metrics` at slice 5c. `server_maxprocs` (a Go runtime concept) and the two `replay` families (traffic replay is unsupported in Rust mode) are retired outright instead. | `pkg/metrics/metrics.go:setupMonitor`; `lib/util/systimemon/systime_mon.go`; `pkg/metrics/server.go:85-99` (the two monitor counters); `pkg/balance/observer/metrics.go`; `pkg/balance/factor/factor_balance.go`; `pkg/balance/router/metrics.go`; `pkg/manager/elect/election.go` | `dataplane::observability::system_time_monitor_counts_jumps_and_heartbeats_like_go`, `native_exposition_matches_go_golden`; `tiproxy-rs::a_failed_startup_stops_the_system_time_monitor`; `tests/dataplane/metrics/native-families.json` (list asserted equal to `METRIC_SPECS`, oracle rejects duplicates and ungathered entries) | `dataplane` | `PARITY-MTR-006` | GO-VERIFIED / RUST-IMPLEMENTED (12 of 12. The two `monitor` families reproduce Go's loop rule — 100ms sampling, jump when the wall clock reads earlier after the wait, callback every tenth tick, keepalive every fifth callback — and run in the real process under the startup guard's stop/join. `balance` 4 (`b_conn` and the three migration families) render from router and process state at scrape; see MTR-007 for `b_conn`'s declared divergence. `backend` 3 (`b_status`, `ping_duration_seconds`, `health_check_seconds`) render from the topology health history: values process-level as Go's collectors are, transition edges and the down-retention clock per observer as Go's `curBackends`/`downBackends` are. `ping_duration_seconds` publishes at the connect, inside the dial retry loop, where Go calls `setPingBackendMetrics` — not at probe return, which would be a greeting read late. `server_owner` renders from the elections the metric-reader campaign holds; see MTR-010 for the labels Rust cannot produce. `b_score` renders from the retained per-factor scores, written at real scoring events behind Go's per-instance ten-second check and cleared when a configuration generation is adopted. `backend_metric` renders from the raw observations the resource and health factors accept, with no throttle and no reset, as Go has neither for it. `backend_metric`'s publication path is covered by `control-router::backend_metric_publishes_accepted_values_labelled_by_address`, which drives the real `evaluate` with **two** inputs — `evaluate_window` runs the resource factors only under a non-Connection policy with more than one input, so a single-input fixture produces a scored row without reaching any publication branch, and a row is therefore not evidence of a write. Each input's opaque routing id, Prometheus instance label and dial address differ, and the cpu samples differ from their average, so a wrong label or the latest-instead-of-average value fails it. The cross-family retirement `DelBackend` performs is ported: a backend down past `backendMetricRetention` is retired from the health families and, through `BackendRetirement`, from `b_conn`'s retained set and every migration label set naming it at either end — metric state only, never the ledger. `health_check_seconds` spans less than Go's cycle does; see MTR-008) | P1 | P1 |
| MTR-007 | `tiproxy_balance_b_conn` is served by summing the physically owned connections per backend address across every router incarnation the process still holds, including one retired by configuration that still owns sessions. **This deliberately differs from Go when two namespaces share a backend address.** Go writes the gauge with `Set` from each namespace's own router, so the last writer decides and the series reports one namespace's count rather than the process total; summing makes the address label mean what it says. The count is physical active only — not reserved, not incoming — and an accepted migration keeps counting against its source until it settles successfully, matching Go moving its `connList` entry only on success. | `pkg/metrics/balance.go` (`b_conn`); `pkg/balance/router/metrics.go:12` (`Set`); `pkg/balance/router/group.go:603,619` (physical `connList` length); `pkg/manager/namespace/namespace.go:20` (one router per namespace) | `control-router::physical_connections_follow_gos_conn_list_exactly`; `control-router::a_backend_address_keeps_reporting_after_its_last_connection_closes` and `::an_address_connected_and_closed_between_scrapes_still_reports` (the zero is retained, and registration happens when the connection lands rather than when the exposition reads); `dataplane::native_exposition_matches_go_golden`. The declared difference is held from both sides: `pkg/metrics.TestBackendConnGaugeOverwritesAcrossNamespaces` pins Go reporting 1 for two namespaces sharing one address, and `control-router::migration_snapshot_sums_shared_backend_address` pins Rust reporting 2 for the same situation. Non-overlapping addresses stay strictly compared by the golden. | `dataplane` | `PARITY-MTR-007` | GO-VERIFIED / RUST-DIVERGENT-BY-DESIGN (deviation limited to shared backend addresses; reversible if the overwrite semantics are ever wanted) | P1 | P1 |
| MTR-008 | `tiproxy_backend_health_check_seconds` reports how long one health check cycle took. **This deliberately differs from Go in what the cycle contains.** Go's observer loop fetches the backend list and then probes in one iteration, and times the whole of it from before `GetBackendList`, so its value includes discovery. Rust has no such iteration: the topology refresh is a separate task on its own `ROUTING_REFRESH_INTERVAL` ticker (`run_refresh` → `poll_merged_topology`), and the health loop is handed an already-published source. The Rust value therefore covers the probe round alone and is systematically smaller than Go's whenever discovery is slow. This is not a timestamp that could be moved earlier: the two loops have different periods, and a health round may run with no refresh at all in between, so there is no instant in the health loop that corresponds to Go's start. Everything else about the family matches — one process-wide gauge, written at the end of every completed round including the ones whose backend-list fetch failed. | `pkg/balance/observer/backend_observer.go:95-109` (`startTime` before `GetBackendList`, `HealthCheckCycleGauge.Set(cost)` at the bottom of every iteration); `pkg/metrics/backend.go:65` | `control-topology::skipping_a_round_leaves_every_status_untouched` (the cycle gauge is written on a round that applied no verdict); `dataplane::native_exposition_matches_go_golden` | `control-topology`, `dataplane` | `PARITY-MTR-008` | GO-VERIFIED / RUST-DIVERGENT-BY-DESIGN (deviation limited to what the interval spans; closing it would mean giving Rust a combined discovery-plus-probe loop it does not have) | P2 | P2 |
| MTR-009 | After a retention purge deletes a backend's series, a migration that was already in flight and then settles recreates them. **The recreated `tiproxy_balance_pending_migrate` value deliberately differs from Go.** Go's `pending_migrate` is an accumulated gauge: `Inc` at issue, `Dec` at settle. Deleting the child discards whatever it had accumulated, so the recreated child restarts at `0` and every later `Inc`/`Dec` applies from there. **Go's value is therefore offset by `-P`, where `P` is the count that label set held when the delete ran**, and it stays offset for the life of the recreated child. Persistently, not permanently: the next delete or reset of that child re-baselines it (to the truth when nothing is in flight, to a new offset otherwise). What it will not do is correct itself — a scrape does not correct it, because a scrape only reads. A later delete resets the accumulation baseline rather than correcting it: with nothing in flight at that moment the recreated child starts from a zero that is also the true count, and the offset is gone; with migrations still in flight it starts from zero against a non-zero real count, and the offset is replaced by a new one. A process restart clears it for the same reason a delete does, and with the same condition. Rust carries no offset: its pending value is read from the router's ledger, which holds the true in-flight count and cannot go negative. CodexM5 ran the real Go path — `DelBackend` then `addMigrateMetrics` — and observed `-1` for the single-migration case, which is this rule at `P = 1`; with `P` migrations in flight across the purge the gap is correspondingly larger and Go can report any value down to `-P`. The recreation itself matches: both ends of the migration get their `b_conn` series back (Go writes both through `removeConn(from)` and `addConn(to)`) and the pending label set is re-admitted, so the divergence is confined to this one family's value. | `pkg/balance/router/metrics.go:31-34` (`addMigrateMetrics`: `Inc` then `Dec`); `pkg/balance/router/group.go:616,625` (`setBackendConnMetrics` on both remove and add); `pkg/metrics/metrics.go:147` (`DelBackend`) | `control-router::retiring_a_backend_deletes_series_without_disturbing_the_ledger` (both addresses return, the pending label set returns, and the ledger's own pending map and session accounting are untouched throughout) | `control-router` | `PARITY-MTR-009` | GO-VERIFIED / RUST-DIVERGENT-BY-DESIGN (deviation limited to the post-purge pending value and persisting while the recreated child lives; reproducing Go's offset would mean accumulating decrements instead of reading the ledger, i.e. deliberately reporting a count the router is not in) | P2 | P2 |
| MTR-010 | `tiproxy_server_owner` reports `1` per election this process holds, labelled with the etcd key trimmed of `/tiproxy/` and `/owner`, and the child is **deleted** on retirement rather than set to `0` — Go's own comment is that a retired owner should not show on the dashboard at all, and `0` would be the different claim that the election is held by nobody here rather than not this process's to report. Rust matches that, and the label is derived from the same `election_name(cluster, zone)` the campaign runs on rather than typed independently. **The set of elections differs from Go: Rust reports only `metric_reader*`.** Go elects for two jobs, the metric reader and the VIP manager (`pkg/manager/vip/manager.go`), and Rust has no VIP manager at all, so a `vip/...` label can never appear. This is a missing subsystem, not a metric decision; the family will gain those labels if and when VIP is ported. | `pkg/manager/elect/election.go:189-202` (`onElected` sets 1, `onRetired` calls `DeletePartialMatch`), `:88` (`trimedKey`); `pkg/metrics/server.go:53`; `pkg/balance/metricsreader/backend_reader.go:40-43` (the four key shapes); `pkg/manager/vip/manager.go:23` (the second election Rust does not have) | `control-topology::the_label_is_derived_from_the_key_the_campaign_uses` (the campaign's key function composed with the metric's label function, over all four Go shapes); `::retiring_deletes_the_series_rather_than_zeroing_it`; `dataplane::native_exposition_matches_go_golden` (the Go oracle wins and then retires a third election, so the absence is exercised rather than assumed) | `control-topology`, `dataplane` | `PARITY-MTR-010` | GO-VERIFIED / RUST-IMPLEMENTED (metric_reader elections only, pending a VIP manager) | P2 | P2 |
| MTR-011 | A metric write made under a routing generation must not outlive that generation's revocation. Rust has generation fencing where Go has none, so this is an invariant Rust owes itself rather than a parity requirement — but a fence that is checked and then acted on is not one: reading `is_live` and then writing leaves the revocation free to land between the two, and holding the metric store's own lock across both only orders the write against *other writes*, never against the revocation. The primitive is `CommitPermit`: `revoke()` takes a short commit lock before clearing validity, `commit` takes it around the caller's effect and runs it only if the permit is still valid, and `commit_all` takes a whole conjunction in one call — address-ordered and deduplicated — because the permits are **not reentrant** and staging them would nest. The guarantee is symmetric and has no third case: a revocation ordered first refuses the commit, a commit ordered first completes before the revocation lands. A value committed under a live generation is **kept** when that generation is later revoked: it was a real observation, and retracting it would replace the family's last-Set and retention rules with "disappears whenever routing re-publishes". **Two distinct authority boundaries, deliberately not merged.** The SQL dial (`ping_duration_seconds`) and `b_status` commit under the **routing-generation gate** alone, which is the authority those writes actually have. `b_score` and `backend_metric` commit under the **whole candidate conjunction** — owner, readiness, configuration snapshot, that namespace's own authority, and the five the backend source contributes — taken in one `commit_all`, and still inside `MetricSnapshot::with_current`, so the feed is held across it (lock order feed → authority → history). `backend_metric`'s values are recorded during evaluation past Go's guards and published in that same closure beside `b_score`, because both describe one scoring round; a round whose commit is refused is discarded rather than queued. **Sharing an authority boundary is not a cross-history atomic scrape snapshot** and is not claimed as one. The health cycle gauge and the retention purge stay deliberately outside any fence, because Go runs both on every observer iteration including the ones whose backend-list fetch failed, and gating them on a source would filter out that behaviour rather than fence anything. | `control-plane::permit::CommitPermit::{revoke, revoke_once, commit, commit_all}`; `control-plane::ownership::LeaseState::release`; `control-plane::runtime::ControlRuntime::{set_phase, drop}`; `control-config::source::ConfigNamespaceSnapshot::{permit, namespace_permit, revoke_retired_namespaces}`; `control-external::GenerationGate::{revoke, try_commit}`; `control-topology::backend_health::probe_sql_port`; `control-topology::health_loop` (the `apply_round` commit); `control-topology::BackendSourceHandle::{commit_current, authorities_for}`; `control-router::authority::Sources::commit_valid`. Go has no counterpart: `pkg/balance/observer` and `pkg/balance/factor` write unconditionally. | Per-step independent receipts. Where an old-head or mutation control is recorded below it was run against the exact property named, but not every step's receipt includes one — the configuration step's receipt is two positive fixtures. **Primitive**: `control-plane::permit::tests::{a_revocation_ordered_first_refuses_the_commit, a_commit_ordered_first_completes_before_the_revocation, commit_all_refuses_when_any_permit_is_revoked, commit_all_deduplicates_and_orders, revoke_once_reports_the_transition_to_a_single_caller}`. **Owner**: `control-plane::ownership::owner_permit_tests::{an_effect_is_refused_once_the_lease_is_released, a_holder_can_commit_but_cannot_end_the_lease, an_already_invalid_permit_does_not_skip_unregistration, a_concurrent_release_revokes_before_returning_even_under_an_in_flight_commit}`. **Readiness and publisher end-of-life**: `control-plane::runtime::readiness_permit_tests::*`. **Configuration**: `control-config::source::config_permit_tests::*` and `::namespace_permit_tests::{a_retired_namespace_loses_its_authority, an_altered_namespace_loses_its_authority, an_unrelated_change_keeps_every_namespace_authority}`. **Topology**: `control-topology::static_source::tests::{commit_current_runs_only_under_the_whole_authority, review_namespace_removal_cannot_land_inside_an_admitted_commit}`; `control-topology::health_history::tests::{a_committed_value_survives_its_generation_being_revoked, b_status_is_committed_under_its_source_gate}`. **Router**: `control-router::factors::tests::a_round_that_never_commits_never_publishes`; `control-router::tests::review_step5_real_router_revocation_refuses_both_metrics_and_throttle`, which drives the real route and balance paths, holds the round between evaluation and commit, publishes an actual new configuration from another thread, and pins that the refused values reach neither family and do not advance the score throttle. | `control-plane`, `control-config`, `control-external`, `control-topology`, `control-router` | `PARITY-MTR-011` | GO-VERIFIED / RUST-IMPLEMENTED (closed for the router-side families at slice 5d. `b_score` and `backend_metric` are no longer check-then-write: `Sources::commit_valid` resolves every identity condition and releases every watch borrow first, then takes owner, readiness, configuration, the namespace's own authority and the backend source's five in a single `commit_all`. The per-namespace authority exists because binding to the snapshot-wide configuration permit would refuse commits on namespaces an unrelated generation never touched; it is carried forward on the same struct-equality rule as `namespace_identities` and revoked before the retired or altered snapshot becomes visible. The commit closure reaches no publisher or watch lock — it takes only the metric histories' own mutexes, which are innermost and acquire nothing further, which is what keeps it acyclic rather than any lock-freedom) | P2 | P2 |

## Shutdown and failure lifecycle

| ID | Required behavior and invariant | Go source | Go test evidence | Rust owner | Target test ID | Status | Canary | Cutover |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| LIFE-001 | Pre-close first waits for load-balancer drain, then closes every listener, then asks sessions to close only at transaction/prepared-state safe points for the configured grace period. | `pkg/proxy/proxy.go:PreClose`; `pkg/proxy/backend/backend_conn_mgr.go:GracefulClose`, `tryGracefulClose` | `pkg/proxy/proxy_test.go:TestGracefulCloseConn`, `TestGracefulShutDown`; `pkg/proxy/backend/backend_conn_mgr_test.go:TestGracefulCloseWhenIdle`, `TestGracefulCloseWhenActive`; `rust/crates/dataplane/tests/session_engine.rs:coordinated_shutdown_stops_accept_then_drains`, `drain_closes_idle_session_and_completes`, `drain_force_deadline_preempts_in_flight_command`; `pkg/controlbridge/bridge_test.go:TestBridgeOperatorDrainLifecycle` | `dataplane`, `control-proto`, `session-core` | `PARITY-LIFE-001` | GO-VERIFIED / RUST-PARTIAL (DPL-04: coordinated stop-accept → safe-boundary graceful drain → absolute-deadline force → join, locally via SIGTERM (`tiproxy-rs` main + `DataplaneHandle::stop_accepting` drain mode) and remotely via the CTL-06 drain path with the Go operator entry `Bridge.StartDrain` / `/api/dataplane/drain`; force preempts stalled mid-command sessions under the cleanup budget with proxy-shutdown attribution) | P0 | P0 |
| LIFE-002 | Force close interrupts in-flight client I/O, is idempotent and race-safe, and lets normal teardown release buffers and unregister routing state. | `pkg/proxy/backend/backend_conn_mgr.go:ForceClose`, `Close`; `pkg/proxy/proxy.go:Close` | `pkg/proxy/backend/backend_conn_mgr_test.go:TestForceClose`, `TestForceCloseUsesGracefulClose`, `TestCloseWhileExecute`, `TestCloseWhileGracefulClose`; `rust/crates/dataplane/tests/session_engine.rs:forced_shutdown_with_stuck_backend_joins_everything_in_budget`; `rust/crates/dataplane/tests/control_dispatch.rs:local_drain_runs_graceful_then_force_and_replays_idempotently`, `force_close_marks_only_on_delivery`; `rust/crates/dataplane/tests/session_loop.rs:terminal_cleanup_budget_and_source_release_order`, `pending_stimuli_shutdown_precheck_dominates`, `three_select_arms_race_cleanly`, `redirect_command_shutdown_interleavings_never_deadlock`; `rust/crates/dataplane/src/server.rs:actual_address_keepalive_and_shutdown_cleanup`, `owner_cancellation_aborts_children_and_releases_every_resource` | `dataplane`, `proxy-io` | `PARITY-LIFE-002` | GO-VERIFIED / RUST-VERIFIED (**interrupts in-flight I/O**: with a permanently stuck backend, session shutdown and server shutdown back-to-back return the server with every listener, session owner, loop and engine joined or aborted inside ONE force budget -- `session_engine.rs:1029` arms a single absolute deadline at the force signal and hard-cancels the engine task, and the sockets drop with it; on the client side the parked session's socket reads EOF after `handle.shutdown()`. **Idempotent and race-safe**: the CLOSED terminal is emitted exactly once across that double shutdown, a force `CloseImmediate` is marked delivered only when the send succeeds (a jammed channel retries on the next tick instead of losing the close), a drain replay never relabels its terminal, and the loop's shutdown precheck plus the three-arm select race both end in one clean teardown. **Normal teardown releases buffers and unregisters routing state**: after shutdown the connection registry is empty and `connection_buffer_bytes` is 0 with no gauge drift, and terminal cleanup stops the pump and drops the transport source before the teardown children finish) | P0 | P0 |
| LIFE-003 | Close cancels signal processing, reports any outstanding redirect result, notifies handshake and router hooks once, closes backend/client sockets, and records lifetime/disconnect source. | `pkg/proxy/backend/backend_conn_mgr.go:Close`; `pkg/proxy/client/client_conn.go:Close` | `pkg/proxy/backend/backend_conn_mgr_test.go:TestCloseWhileRedirect`, `TestRedirectAbortedByCloseReportsFail`, `TestCloseWhileConnect`; `pkg/proxy/net/packetio_test.go:TestPacketIOClose`; `rust/crates/dataplane/tests/session_engine.rs:close_command_terminal_is_exact`, `session_path_emits_query_traffic_and_exact_quit_source`, `backend_greeting_transport_break_is_backend_network_on_both_observables`, `backend_to_client_destination_write_break_is_client_network_on_both_observables`; `rust/crates/dataplane/tests/control_dispatch.rs:review_local_terminal_from_session_close_survives_eviction`; `rust/crates/control-router/src/tests/migration.rs:review_force_close_publishes_migration_terminal`, `migration_simulations_never_share_ownership_and_close_wins_terminal_races` | `dataplane`, `control-proto` | `PARITY-LIFE-003` | GO-VERIFIED / RUST-VERIFIED (**cancels signal processing**: the loop's `next_action` precheck observes a pre-existing shutdown before entering the select, so queued control commands and relayed transport boundaries do not get one more turn. **Reports any outstanding redirect result**: `session_engine.rs:1013` settles the local redirect envelope with `finish_redirect(false)` while the engine still owns the route lease -- selector-drop stays an abort backstop -- and on the owner side a force close publishes the migration terminal and wins terminal races against a concurrent settle without two owners ever sharing it. **Notifies hooks once**: a per-connection `CloseCommand` resolves under its exact admitted id with the initiating request id, and the terminal produced by the `session_closed` completion path survives tombstone eviction without a replay overwriting it. **Closes backend/client sockets**: the same test asserts the client socket closes. **Records lifetime/disconnect source**: the real session path emits the legacy command and quit-source labels, and a break at the backend greeting versus a break writing to the client classify as backend- versus client-network on both observables. The lifetime *duration* is Rust's too, from the same close observation: `dataplane/src/observability.rs:1653` increments `tiproxy_server_disconnection_total{type=<source>}` and observes `tiproxy_session_conn_lifetime_seconds` together, and `native_exposition_matches_go_golden` holds that rendering byte-identical to the Go `promhttp` oracle for the recorded deltas, including the `client network break` disconnection label) | P0 | P0 |
| LIFE-004 | Recover managed background-task panics, close affected connection resources, and keep listener/config loops cancellable without orphan tasks. | `pkg/proxy/proxy.go:Run`; `pkg/proxy/backend/backend_conn_mgr.go:Connect`; `pkg/util/waitgroup/` | `pkg/proxy/proxy_test.go:TestRecoverPanic`; `pkg/proxy/backend/backend_conn_mgr_test.go:TestProcessSignalsPanic`; `rust/crates/dataplane/src/server.rs:handler_panic_is_contained_and_raii_cleanup_runs`, `owner_cancellation_aborts_children_and_releases_every_resource` | `dataplane` | `PARITY-LIFE-004` | GO-VERIFIED / RUST-VERIFIED (**panic containment**: an injected panic in the first connection's handler closes that connection's socket and nothing else -- the *second* connection is served normally under id 2, the registry holds exactly it, `handler_panics_total` is 1, and the server still returns `Ok` from `run`. Rust needs no `recover` wrapper for this: the handler runs in its own task, so the panic is contained by the task boundary and the connection's resources are released by the lease's `Drop` rather than by an explicit cleanup the panic could skip. **Cancellable without orphans**: aborting the listener owner mid-session still drains the registry to empty, drops the in-flight client to EOF, refuses new connects, and leaves `active_connections` and `connection_buffer_bytes` at 0 -- no task outlives the owner holding a connection. A listener owner that panics or exits early is a typed `ServerError::ListenerTaskPanicked` / `ListenerTaskStopped` from `run`, not a silent degradation) | P1 | P0 |
| LIFE-005 | Preserve normal versus abnormal quit classification for client quit, client network, backend network, backend SQL error, proxy shutdown, handshake, and control-hook failures. | `pkg/proxy/backend/error.go`; `pkg/proxy/client/client_conn.go:Run`; `rust/crates/session-core/src/error_source.rs`; `rust/crates/dataplane/src/session_engine.rs` | `pkg/proxy/backend/backend_conn_mgr_test.go:TestReturnMySQLError`, `TestDisconnectLog`, `TestHandlerReturnError`; `pkg/proxy/backend/cmd_processor_test.go:TestNetworkError`; `session_core::error_source::tests`; `dataplane::observability::quit_source_labels_match_go_exactly`; `rust/crates/dataplane/tests/session_engine.rs:session_path_emits_query_traffic_and_exact_quit_source` | `session-core`, `dataplane` | `PARITY-LIFE-005` | GO-VERIFIED / RUST-IMPLEMENTED | P1 | P1 |

## Phase-1 Rust route-owner inventory

`CONTROL_CAPABILITY_RUST_ROUTE_OWNER` (6) is the production compatibility
fence. The bundled pair requires it at handshake; a missing capability rejects
the session and never selects Go as a fallback owner. Once negotiated, this is
the exact bridge inventory:

| Family | Production count/effect | Retained reason |
| --- | --- | --- |
| `StateSnapshot` / `SnapshotResult` | Residual only: backends/namespaces empty; config carries static MySQL capability/version facts. Nonempty route state is rejected. | Snapshot/static acknowledgement until an owner-specific protocol v2. |
| `Handshake*`, `Route*`, `ConnectionEvent`, `Redirect*`, `Close*`, `Drain*` | Zero. An injected body increments `rust_legacy_route_violation` (the count includes retired drain bodies) and leaves the route-state hash unchanged. | Numeric v1 tags/types are non-actionable tombstones; operator drains are issued inside the Rust process (CP-ADMIN slice 3). |
| `ReconcileRequest` / `ReconcileSnapshot` | Connections empty, connection-event sequence zero, response connections empty; no route callback/effect. | Snapshot, metrics/metering watermarks; the drain watermark is Rust's own diagnostic value. |
| `MetricsBatch`, `MeteringBatch` / `MeteringAck` | Retained on their dedicated owner paths. | Observability and durable CP-METER are not route ownership. |

Production Go constructs the residual handler without `RouterAdapter`, does
not attach a handshake/router/topology fallback, and does not call
`ResolveOrphans`. The legacy adapter and capabilities 1–3 remain Phase-1 test
oracles only. After the full 36-logical/48-physical live qualification, Phase
2 is a separate dead-path deletion PR; it may simplify the residual handler but
must retain the deprecated v1 schema numbers until protocol v2 and may not add
or repair routing semantics.

CP-METER protocol-v2 candidate: add `producer_id` beside
`ReconcileSnapshot.metering_sequence`. In v1 the snapshot is only a
current-session readiness gate and cannot trim a producer-qualified WAL; the
existing `MeteringAck` is the sole safe trim authority. Until v2, the WAL is
the only cross-session owner and every metering wire copy is session-scoped.

## Explicit exclusion

| ID | Excluded behavior | Go source | Required Rust-mode behavior | Rust owner | Target test ID | Status | Canary | Cutover |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| EXCL-001 | SQL traffic capture, capture initialization from `SHOW SESSION_STATES`, replay log output, and replay execution. | `pkg/sqlreplay/`; capture hooks in `pkg/proxy/backend/backend_conn_mgr.go`; `lib/config/proxy.go:EnableTrafficReplay` | Starting or reloading Rust dataplane mode with traffic capture/replay enabled fails fast with an actionable error. No payload or replay record may cross the Go/Rust boundary. | `control-proto`, `dataplane` | `PARITY-EXCL-001` | EXCLUDED | P0 | P0 |

## Audit and sign-off rules

### Rust wire and transport safety decisions

The rows below record intentional Rust wire-codec or transport differences
from permissive, unchecked, or internally inconsistent Go paths. They are not
additional parity items and do not have independent `PARITY-*` test IDs; their
tests remain attached to the related manifest items. A later compatibility
change must update this ledger and add a focused regression test instead of
silently changing the boundary.

| Decision | Related items | Go behavior | Rust behavior | Rationale and compatibility impact |
| --- | --- | --- | --- | --- |
| `WIRE-00-D1` canonical length-encoded integers | HS-003, RSP-001, RSP-003 | `ParseLengthEncodedInt` accepts `0xfc`, `0xfd`, or `0xfe` encodings that use more bytes than the value needs. | Return `DecodeError::NonCanonicalLength`; keep the undefined `0xff` marker as a separate `DecodeError::InvalidValue`. | Issue #17 explicitly requires typed errors for non-canonical lengths. This deliberately rejects a non-canonical peer packet that Go may accept, including in OK packet counters; canonical corpus bytes are unchanged. |
| `WIRE-00-D2` protocol-4.1 ERR marker | RSP-001 | `ParseErrorPacket` skips the byte before SQLSTATE without checking that it is `#`. | Require `#` and return `DecodeError::InvalidValue` otherwise. | Prevent a malformed ERR layout from being reinterpreted as a different SQLSTATE/message boundary. Canonical ERR packets are unchanged. |
| `WIRE-00-D3` initial-greeting invariants | HS-001, HS-011 | `ParseInitialHandshake` indexes the assumed layout without checking protocol version 10 or the zero filler byte. | Require protocol version 10 and filler `0x00`, returning typed errors on mismatch. | Converts unchecked malformed-backend behavior into an explicit handshake failure without changing a valid greeting. |
| `WIRE-00-D4` negotiated connection fields | HS-003, HS-006, CMD-017 | Some missing database/attribute tails or NULL attribute lengths are tolerated, collapsed to empty state, or downgraded to a warning. | Require every capability-selected field to be present and terminated; reject NULL connection-attribute lengths and malformed attribute pairs. | Prevents ambiguous partial identities and attribute boundaries. Valid empty database/attribute values remain representable with their canonical terminator/zero length. |
| `WIRE-01-D1` forwarded physical sequence | PKT-004 | `ForwardPacketTo` and materialized `ForwardUntil` regenerate a destination header, but the no-data `ForwardUntil` fast path copies the source header byte-for-byte while only advancing the destination's sequence tracker. | Every streaming path regenerates the physical header from the destination sequence. | Keeps the destination's wire bytes and tracked sequence consistent. Canonical traffic is unchanged; only a tolerated mismatched source sequence differs. |
| `WIRE-01-D2` empty-packet peek | PKT-005, RSP-005 | `ForwardUntil` always peeks five bytes, so a zero-length physical packet blocks at EOF or reads one byte across the packet boundary; LOCAL INFILE bypasses this path. | Peek the four-byte header first and return `first_byte = None` without reading beyond an empty packet. | Makes empty-packet classification non-consuming and boundary-safe. Non-empty packets are unchanged; callers must handle `None` explicitly. |
| `WIRE-03-D1` compressed output exceeding u24 | CMP-001, CMP-002 | `compressedReadWriter.Flush` truncates `compressedLength` into three header bytes even when a maximum-size incompressible input expands beyond `0x00ff_ffff`, emitting a corrupt frame. | If the codec output cannot fit the 24-bit compressed-length field, emit the original at-most-u24 payload as a raw frame (`uncompressed_length = 0`). | Prevents a valid local input from producing a header that lies about its body. Compatibility changes only the previously corrupt maximum-size incompressible encoding; canonical and compressible frames are unchanged. |
| `WIRE-03-D2` decompression bounds and exact consumption | CMP-001, CMP-002, CMP-003 | The Go reader trusts both header lengths, grows the destination from the declared uncompressed size, copies exactly that many decoded bytes, and does not require the codec stream to end or consume the complete compressed body. | Reject a frame before decoding when its declared output exceeds the absolute/ratio limits; decode into a caller-sized destination; require exact output length, one complete codec frame, and no trailing compressed bytes. | Bounds memory/CPU amplification and rejects ambiguous trailing or length-mismatched hostile frames. Valid Go zlib/zstd frames remain mutually decodable; malformed peers previously tolerated or partially consumed are disconnected. |
| `WIRE-03-D3` zstd implementation trust split | CMP-002, CMP-003 | Go uses `klauspost/compress/zstd` for both directions. The initially selected pure-Rust C translation avoids FFI but contains substantial internal unsafe code, including its parser/decompressor. | Decode all peer-controlled frames with exact-pinned, dependency-free `zstd-zero`, whose crate root forbids unsafe code and whose buffers are caller-owned and bounded. Retain exact-pinned `zstd-pure-rs` only for levels 1–22 encoding; track its residual internal-unsafe risk until a mature, MSRV-compatible, full-level safe encoder is available. | Removes unsafe code from the hostile compressed-frame parsing boundary without weakening negotiated-level compatibility. The remaining encoder receives bounded uncompressed frames and emits bytes that both safe Rust and production Go decoders verify; supply-chain scans alone are not treated as implementation-memory-safety evidence. |
| `WIRE-04-D1` backend hostname verification | TLS-003, TLS-004 | `lib/util/security/cert.go:buildClientConfig` sets `InsecureSkipVerify: true` even with a CA and its custom `VerifyPeerCertificate` runs `x509.Verify` without `DNSName` (the assignment is commented out with a TODO), so a CA-valid certificate for the wrong host is accepted. | Standard WebPKI verification checks the routing-selected backend hostname; a CA-valid certificate for another name is rejected (`skip_ca_verification` remains the explicit opt-out). | Deliberate security strengthening. Compatibility impact: backends presenting CA-valid certificates whose SANs do not cover the dialed host worked through Go and fail through Rust — operators must fix certificate SANs or configure `skip_ca_verification`. Covered by the focused wrong-hostname test. |
| `WIRE-05-D1` PROXY v2 encode length limits | PP2-001, PP2-002 | `Proxy.ToBytes` writes `byte(len>>8), byte(len)` for each TLV and for the aggregate body length, silently truncating either above 65535 and corrupting the frame. | Return `ProxyEncodeError::TlvTooLong` for a single oversized TLV and `ProxyEncodeError::BodyTooLong` when addresses plus TLVs exceed the u16 body-length field. | Prevents emitting a header whose length field lies about its body, at both the per-TLV and aggregate levels. Decode keeps Go's clamping tolerance for truncated TLV declarations. |
| `WIRE-05-D2` PROXY v2 Unix path encode | PP2-001, PP2-002 | `Proxy.ToBytes` writes Unix paths unpadded at any length, while `ParseProxyV2` only reads the spec's fixed 216-byte block — Go cannot round-trip its own Unix headers. | Keep the unpadded write for paths up to 108 bytes and return `ProxyEncodeError::UnixNameTooLong` above that. | Preserves Go's observable output for representable paths and rejects only inputs Go would corrupt. The decode side keeps Go's fixed-216-byte expectation, short-body tolerance included. |
| `SES-02-D1` unterminated auth-switch plugin name | HS-004, HS-005 | The auth-forward loop slices `serverPkt[1 : bytes.IndexByte(serverPkt[1:], 0)+1]`; a backend auth-switch packet without a NUL terminator makes `IndexByte` return `-1` and the slice expression panic. | `classify_backend_auth_packet` returns a typed `MalformedAuthPacket` (static reason, no payload bytes) for the missing terminator; the session treats it as a malformed-packet failure. | A hostile or broken backend must not crash the proxy. Canonical backends always NUL-terminate, so observable behavior for valid traffic is unchanged; only Go's panic path differs. |
| `SES-03-D1` malformed special-command prefixes and values | CMD-023, CMD-024, CMD-025, CMD-026, CMD-027, CMD-028 | Statement-status and `COM_SET_OPTION` paths index fixed two- or four-byte fields after forwarding; a truncated packet can panic. An unsupported set-option value is discovered only after the backend request/response completed. | Dispatch validates statement IDs and option widths/values before forwarding, returning a typed payload-free error. Trailing bytes remain tolerated. | Converts hostile client panic paths into deterministic malformed-command failures and prevents an already-invalid option from reaching the backend. Every valid command is unchanged. |
| `SES-03-D2` command sentinel and extensions | CMD-032 | `COM_END` and higher bytes enter the generic forwarding branch, but later index the `[ComEnd]` metrics array and can panic out of bounds. | Reject `0x20..=0xff` before backend forwarding or metrics access under the fixed `UnknownCommandPolicy::Reject`. | Resolves the command-table sentinel decision without copying an unsafe Go path. Future command support requires an explicit enum/response-policy addition instead of accidental transparent forwarding. |
| `SES-04-D1` malformed terminal response prefixes | RSP-001, RSP-002, RSP-003, RSP-008 | `ParseOKPacket`, `ParseErrorPacket`, and `handleEOFPacket` index assumed structured fields and can panic on truncated backend packets selected by their header byte. `ParseOKPacket` reads only through status, so a protocol-4.1 OK missing its required warning count is also accepted. | Parse the bounded structured prefix first and return a typed, payload-free `ResponseObserverError::MalformedPacket` without mutating observer state; require the complete canonical protocol-4.1 OK layout through warnings. | A hostile or broken backend must not crash the proxy, partially apply status, or supply a structurally incomplete terminal packet. Canonical OK/ERR/EOF bytes and every valid response boundary are unchanged. |
| `SES-05-D1` malformed prepare metadata framing | RSP-006, PS-004 | `forwardPrepareCmd` indexes the assumed prepare-OK fields without checking their length, then forwards the declared number of metadata packets without validating the two classic EOF slots; a truncated header can panic and a malformed metadata terminator can be accepted. | Require the complete canonical 12-byte prepare-OK layout, count the two metadata groups explicitly, and validate each required classic EOF before committing metadata; return a typed payload-free error without mutating observer state. | A hostile or broken backend must not crash the proxy or make an invalid prepare response look complete. Canonical prepare responses in both EOF modes retain identical bytes and their single flush boundary. |
| `SES-08-D1` strict internal-result metadata | RSP-007, MIG-002 | The go-mysql `ReadResultSet` / `GetStringByName` path accepts a NULL column count, does not require the column-definition fixed-field length to be 12 or consume all definition bytes, and resolves duplicate names to the first match. | Reject a NULL column count, non-12 fixed-field length, trailing column-definition bytes, and duplicate column names with typed payload-free errors. | A hostile or broken backend must not make malformed or ambiguous session state look valid. Canonical migration state/token results are unchanged; only malformed metadata that Go may tolerate is rejected fail-closed. |

Before changing an item to `PARITY-VERIFIED`:

1. Add its exact target test ID to the language-neutral corpus or integration
   suite. A broad suite name without a selectable case is insufficient.
2. Run the same observable inputs against the recorded Go oracle and Rust
   dataplane. Compare packet bytes, flush/close boundaries, state, routing
   events, metrics, and error classification as applicable.
3. Record Go commit, Rust commit, TiDB version, driver version, TLS/compression
   mode, and architecture in the test artifact.
4. Resolve every `DECISION-REQUIRED` row. Rust must not accidentally preserve a
   panic, security weakness, or undefined Go behavior.
5. Require all P0 items before any production canary, all P0/P1 items before
   cutover, and an explicit owner plus expiry for any P2 exception.

### Protocol review

The acceptance review must be performed by a TiProxy/MySQL protocol reviewer
after comparing this manifest with the source inventory above. Reviewers should
confirm command coverage, EOF/capability combinations, prepared/cursor state,
migration safety, and topology/lifecycle hooks rather than reviewing prose only.

| Reviewer | Revision reviewed | Result | Date | Notes |
| --- | --- | --- | --- | --- |
| bb7133 (TiProxy/MySQL protocol reviewer) | `e3371098480c4d644b070544f9c1aef63fcee9b1` (parity drift gate green through current `main`) | PASS — no P0/P1 behavior missing | 2026-08-25 | Review support: independent source enumeration diffed row-by-row against this manifest; evidence and clarifying findings F1–F8 recorded in [issue #2](https://github.com/bb7133/tiproxy/issues/2#issuecomment-5408354185). F1 (SSL capability stripped via XOR) and F2 (PPV2 classification depends on TiDB error wording) are annotated on TLS-001 and PP2-003 below. |
